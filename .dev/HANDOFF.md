# Big Cycle Atlas — Development Handoff

Last updated: 2026-09-10
Current version: 0.1.0
Current branch: main

## Current state

**SPRINT 6.1 COMPLETE (2026-09-10): Indebtedness Composition Methodology Audit — methodology/research sprint, NO implementation. pytest 526 unchanged; DB unchanged (6814 / 27 / 22 / 10 / 1872); model versions unchanged (normalization-v0.6, force-aggregation-v0.1); no force code changes, no phase, no cycle composite, no force confidence, no persistence, no API, no commit/push.** Verdict: **DEFER_INDEBTEDNESS_COMPOSITION** (DEC-030). Two empirical blockers: (1) government-debt normalization is a hard prerequisite — the stock measure is the dominant differentiator when DSR is similar (JPN 214% debt/GDP vs CHE 39% have comparable DSR stress levels; any composition ignoring government debt is a two-input formula masquerading as the force); (2) no defensible penalty-shape interaction between DSR (base condition) and credit-gap (vulnerability penalty) is approved — all five candidate structures (A capped deduction, B multiplicative dampener, C gate/regime, D worst-component/min, E DEFER) either require arbitrary parameters, mishandle the credit-gap neutral-50 semantics (DEC-021: 50 = neutral no-excess, NOT strength), or improperly ignore government debt. Empirical tracked_8 profile (2025-Q2, read-only): credit-gap excess (CG<50) is rare (only JPN, level 35.9); DSR stress (level<50) is common (CHN 1.0, CHE 16.7, FRA 6.4, JPN 41.2, IND 49.0); government debt spans 20%–229%. Missingness policy: any future composition requires ALL THREE components present — any missing → force level None. DEC-030 created. Files changed: `.dev/DECISIONS.md` (DEC-030), `.dev/FORCE_AGGREGATION.md` (audit note), `.dev/PROJECT_STATUS.md`, `.dev/BACKLOG.md`, `.dev/HANDOFF.md`, `.dev/DEVLOG.md`. Recommended Sprint 6.2: government-debt normalization methodology audit (the hard prerequisite).

**SPRINT 5.22 COMPLETE (2026-09-10): First Executable ForceSignal Layer + Milestone 5 Closeout — implementation sprint. pytest 520 passed (476 baseline + 44 new); indicator normalization stays `normalization-v0.6`; force aggregation stays `force-aggregation-v0.1`; confidence stays None; backtest_safe False; no force persistence, no public force API, no commit/push.** Implements the first executable ForceSignal layer using ONLY the methodology approved in Sprint 5.21 / DEC-029. Part 0 hardening: (1) PROJECT_STATUS stale sections repaired (removed stale "~97%" language, updated to milestone language); (2) FORCE_COVERAGE current sections updated (Education live=TERTIARY_ATTAINMENT_25_34 PARTIAL, Indebtedness live=DSR+CREDIT_TO_GDP_GAP+GOVERNMENT_DEBT_GDP, Wealth gaps live=GINI_INDEX+WEALTH_SHARE_TOP_10 PARTIAL, WGI diagnostics 1872 rows imported); (3) live-input config completeness guard (every force live input MUST have a component role — fail loudly at import); (4) real duplicate force-config detection (tuple-first + _build_config_dict with duplicate check BEFORE dict construction); (5) proxy-condition ceiling guard (PROXY_CONDITION identity requires coverage_ceiling=PARTIAL); (6) dimension approval guard (relative/momentum identity_copy only on WGI x3 — explicit typed approved-dimension mapping for v0.1). Part 1: ForceSignal dataclass (app/cycle/force_signal.py) — non-persisted, score range validation (never clamp), relative provenance copy, backtest_safe False, confidence None. Part 2: pure aggregate_force_from_signals — IDENTITY_SINGLE copies level/relative/momentum exactly (no rescale/weight/average), None propagates independently per dimension, DEFERRED_MULTI returns None with component provenance preserved, SUPPORTING_CONTEXT never contributes numerically. Part 3: build_force_signals_as_of service — calls normalize_indicator_as_of for configured components, returns exactly 17 forces, coverage from existing service, no DB writes. 3/17 forces executable (Rule of law, Corruption, Internal conflict proxy); 14/17 intentionally None. Live read-only smoke (2025-Q2, CHE/USA/CHN/IND): all 17 forces returned, 3 identity forces have scores, Indebtedness coverage=available + level=None, internal_conflict stays PARTIAL, no writes. Files changed: `apps/api/app/cycle/force_signal.py` (NEW), `apps/api/app/services/force_signal_service.py` (NEW), `apps/api/app/cycle/force_aggregation_definitions.py` (hardened), `apps/api/tests/test_force_signal.py` (NEW, 28 tests), `apps/api/tests/test_force_signal_service.py` (NEW, 16 tests), `apps/api/tests/test_force_aggregation_config.py` (7 new hardening tests), `apps/api/scripts/smoke_force_signals.py` (NEW, read-only), `.dev/FORCE_AGGREGATION.md`, `.dev/FORCE_COVERAGE.md`, `.dev/HANDOFF.md`, `.dev/PROJECT_STATUS.md`, `.dev/BACKLOG.md`, `.dev/DEVLOG.md`. **Milestone 5 COMPLETE**: indicator normalization + first defensible executable force layer. 3/17 numeric identity forces, 14/17 intentionally unscored.**

**SPRINT 5.20.1 COMPLETE (2026-09-10): Source Identity + Raw Provenance Hardening — correctness sprint before force-aggregation methodology. pytest 445 passed (435 baseline + 10 new); model version stays `normalization-v0.6`; confidence stays None; no force aggregation, no force scores, no normalization curves, no commit/push.** Two provenance weaknesses fixed: (1) OECD education provider identity was abbreviated because `SourceSeries.external_code` varchar(100) was too short — the exact identity (agency + dataflow + version + 17-dimension SDMX key) is 140 chars. Alembic `a8f3c2d1e5b7` widens the column to varchar(255). The `EAG_LSO_NEAC/...` abbreviation is retired; the exact serialized identity `OECD.EDU.IMEP,DSD_EAG_LSO_EA@DF_LSO_NEAC_DISTR_EA,1.0/{cc}...` is now stored (mirrors the OECD SDMX REST URL path). The existing SourceSeries row was updated in place by seed — no duplicate created, SourceSeries stays 22, education observations (200) intact. 8 new source identity regression tests. (2) WID `data_quality` raw provider representation was lost when parsing failed or when the string was converted to int — `raw_payload` now carries both `data_quality_raw` (exact raw string) and `data_quality` (typed int or None). Unknown codes like "A" stay "A" (not None-or-zero); empty string stays "". DEC-027 NO-filtering policy unchanged. 2 new WID data_quality regression tests. Live DB verified: 6,814 observations / 27 indicators / 22 SourceSeries / 10 DataSources / 1,872 diagnostics — all unchanged. Files changed: `apps/api/alembic/versions/a8f3c2d1e5b7_widen_source_series_external_code_to_255.py` (NEW), `apps/api/app/models/source_series.py`, `apps/api/app/data_sources/oecd_mappings.py`, `apps/api/app/data_sources/wid.py`, `apps/api/tests/test_oecd_education.py`, `apps/api/tests/test_oecd_persistence.py`, `apps/api/tests/test_wid_adapter.py`, `apps/api/scripts/verify_oecd_identity.py` (NEW), `.dev/HANDOFF.md`, `.dev/PROJECT_STATUS.md`, `.dev/DEVLOG.md`, `.dev/DATA_SOURCES.md`, `.dev/DECISIONS.md` (DEC-026, DEC-027 clarifications).**

**SPRINT 5.20 COMPLETE (2026-09-10): Education + WID Implementation + Sprint-5.19 Hardening. pytest 435 passed; model version stays `normalization-v0.6`; confidence stays None; no force aggregation, no force scores, no normalization curves, no commit/push.** Five parts: (0) IMF WEO adapter hardened — `math.isfinite()` replaces NaN-only check (rejects NaN/+inf/-inf), fiscal-year century rollover fixed (`FY1999/00`→2000, `FY2099/00`→2100), `LATEST_ACTUAL_ANNUAL_DATA` boundary extracted by attribute ID from SDMX structure (not by position), docstring corrected to slash-separated URL; 7 new regression tests. (A) OECD tertiary attainment age 25-34 — IMPLEMENTED with exact 17-dimension no-wildcard production identity (`EAG_LSO_NEAC/{cc}._T.Y25T34.ISCED11A_5T8._T.POP._Z._T._Z.ED_NED.POP._Z.PT_POP_SEX_AGE.OBS._Z.NEAC.A`), Y25T34 selected over Y25T64 (coverage NOT materially different; 25-34 = recent cohorts), attainment != enrollment preserved (DEC-007), 200 observations across 8 countries (CHN: 1 obs, IND: 8 obs — sparse histories preserved), idempotency verified, Education promoted to PARTIAL; (B) WID top-10 wealth share — IMPLEMENTED via bulk download (882 MB zip, no API key), exact series `shwealj992`/`p90p100` (equal-split adults, pop=`j`), raw fraction 0-1 preserved, data_quality preserved but NOT filtered (official semantics unverified — DEFER policy), 670 observations across 8 countries, idempotency verified, Wealth force stays PARTIAL (DEC-009); (C) `GOVERNMENT_DEBT_GDP` wired into Indebtedness as a live input (was candidate-only), CONTEXTUAL_DEFERRED — no score, no weight. Final DB: 6,814 observations / 27 indicators / 22 SourceSeries / 10 DataSources / 1,872 diagnostics. Files changed: `apps/api/app/data_sources/imf_weo.py`, `apps/api/app/data_sources/imf_weo_mappings.py`, `apps/api/app/data_sources/oecd_mappings.py`, `apps/api/app/data_sources/wid.py` (NEW), `apps/api/app/data_sources/wid_mappings.py` (NEW), `apps/api/app/cycle/force_definitions.py`, `apps/api/app/cycle/normalization_definitions.py`, `apps/api/app/db/seed.py`, `apps/api/scripts/ingest_wid.py` (NEW), `apps/api/scripts/compare_oecd_age_groups.py` (NEW), `apps/api/scripts/inspect_oecd_dims.py` (NEW), `apps/api/scripts/verify_exact_oecd_key.py` (NEW), `apps/api/scripts/print_db_counts.py` (NEW), `apps/api/tests/test_imf_weo_adapter.py`, `apps/api/tests/test_oecd_education.py` (NEW), `apps/api/tests/test_wid_adapter.py` (NEW), `apps/api/tests/test_normalization.py`, `apps/api/tests/test_force_coverage.py`, `apps/api/tests/test_data_sources.py`, `apps/api/tests/test_oecd_persistence.py`, `apps/api/tests/test_gini_cpi.py`, `apps/api/tests/test_military.py`, `apps/api/tests/test_wgi_governance.py`, `apps/api/tests/test_world_bank_mappings.py`, `apps/api/tests/test_world_bank_trade.py`, `apps/api/tests/test_bis_persistence.py`, `apps/api/tests/test_indicators.py`, `apps/api/tests/test_indicator_diagnostics.py`, `apps/api/tests/test_wgi_diagnostic_ingestion.py`, `.dev/DEVLOG.md`, `.dev/HANDOFF.md`, `.dev/PROJECT_STATUS.md`, `.dev/BACKLOG.md`, `.dev/DATA_SOURCES.md`, `.dev/FORCE_COVERAGE.md`, `.dev/NORMALIZATION.md`, `.dev/DECISIONS.md`. Next: Sprint 5.21 — force-aggregation methodology.**

**SPRINT 5.18 COMPLETE (2026-09-10): High-Value Data Gap Closure Audit (DEC-025) — read-only research / documentation sprint. pytest 379 green (unchanged — no code changes); model version stays `normalization-v0.6`; confidence stays None; no migration, no model-version bump, no ingestion, no persistence, no normalization, no scoring, no frontend change. DEC-025 added (durable source-contract decision).** Three tracks audited with official sources only: (A) IMF WEO general-government gross debt `GGXWDG_NGDP` — verified via DataMapper API + SDMX 3.0, all 8 tracked_8 covered, READY for implementation (vintage/forecast risk: DataMapper mixes historical+forecast with no flag; SDMX 3.0 exposes `LATEST_ACTUAL_ANNUAL_DATA`); (B) Education Option C — WB SE.SEC.NENR is STALE (USA/CHE/DEU/GBR last 2017, CHN has no data, WB metadata says "Reference period: 1970-2019"), OECD tertiary attainment `DSD_EAG_LSO_EA@DF_LSO_NEAC_DISTR_EA_MIGR` is triennial (A3) with 5/8 tracked_8 coverage (USA/CHE/DEU/FRA/GBR; JPN/CHN/IND not in dataset), verdict PARTIAL; (C) WID wealth `shweal` (share of net personal wealth) — access verified (bulk download no key, or R-package webservice with API key), all 8 tracked_8 in WID country index, READY as partial proxy but does NOT lift DEC-009 PARTIAL ceiling. Recommended sequence: 5.19 WEO debt → 5.20 OECD attainment → 5.21 WID wealth → 5.22 Force Layer + Milestone 5 Closeout. Files changed: `.dev/DECISIONS.md` (DEC-025), `.dev/DATA_SOURCES.md` (IMF + WID sections), `.dev/FORCE_COVERAGE.md` (Sprint 5.18 audit section + updated priorities), `.dev/PROJECT_STATUS.md`, `.dev/HANDOFF.md`, `.dev/DEVLOG.md`, `.dev/BACKLOG.md`.**

**SPRINT 5.17.1 COMPLETE (2026-09-09): Gini Global-Universe Filter Hardening + Living-Doc Cleanup — empirical-research bug-fix sprint. pytest 379 green (373 baseline + 6 new economy-filter regressions in `tests/test_gini_economy_filter.py`, all offline with mocked WB country metadata — no external HTTP in pytest); model version stays `normalization-v0.6`; confidence stays None; no migration, no model-version bump, no DEC change.** Root cause: the read-only `apps/api/scripts/gini_calibration_profile.py` global Gini universe filter used a hand-written `aggregate_codes` blacklist that wrongly listed real economies ZAF (South Africa) and PSE (West Bank and Gaza) as aggregates. Fix: the filter now uses the AUTHORITATIVE World Bank country-metadata endpoint (`/v2/country`, `region.id != "NA"` => real economy; aggregates carry `region.id == "NA"` and `region.value == "Aggregates"`) via a pure helper `build_valid_economy_codes(country_records)` (no I/O — unit-tested offline with mocked metadata). The GINI record's `countryiso3code` (3-letter) is matched against the metadata `id` (the previous code matched the 2-letter `country.id` against a 3-letter blacklist, so the blacklist was in fact ineffective — but the methodology is now authoritative on both axes). Fail-loudly: if the country metadata cannot be retrieved or parsed reliably, the GLOBAL live profile STOPS — it does NOT silently fall back to the old blacklist; the tracked_8 DB profile may still run. Corrected global counts (read-only live WB API v2, 2026-09-09): 217 real economies identified via metadata; **2430 valid country-year observations, 171 countries, 1963–2025** (same counts as before — the old blacklist was ineffective, but the methodology is now defensible); pooled min=20.2 median=35.3 max=71.1; **ZAF RETAINED (7 obs, 54.1–65), PSE RETAINED (9 obs, 33.7–36.4)**; cross-sections 2000 n=57 median 36.4, 2010 n=86 median 33.7, 2020 n=70 median 34.35; latest sufficiently populated year (n>=30) 2023 n=57 median 33.9; decade medians 34–39; country count by decade 2→160→115. DEC-024 verdict UNCHANGED — the filtering bug did not materially alter the Sprint 5.17 methodology conclusion (the welfare-concept problem and missing per-observation welfare tag remain separate from this filtering bug). Welfare-concept wording refined to "provider-methodology / country-level welfare-concept classification" (NOT "every CHN observation is definitively tagged consumption by the API" — the API does not expose that tag; no invented adjustments). Living-doc cleanup: BACKLOG.md `## Now`, PROJECT_STATUS.md (Phase/Progress/Current milestone/Next milestone/Last successful test), and this HANDOFF updated to current truth (Sprint 5.17/DEC-024 + Sprint 5.16/DEC-023 complete; pytest 379; normalization-v0.6; confidence None; Gini level deferred; no force scores). Files changed: `apps/api/scripts/gini_calibration_profile.py` (authoritative metadata filter + fail-loudly + ZAF/PSE verification + welfare-concept wording refinement + latest-sufficiently-populated-year output), `apps/api/tests/test_gini_economy_filter.py` (NEW — 6 offline regressions: ZAF retained, PSE retained, known aggregates excluded, real-economies set, SSF region-id collision handled, malformed records skipped), `.dev/NORMALIZATION.md` (Sprint 5.17.1 section + corrected counts), `.dev/DECISIONS.md` (DEC-024 impact note), `.dev/DATA_SOURCES.md` (Gini economy-filter clarification), `.dev/HANDOFF.md`, `.dev/PROJECT_STATUS.md`, `.dev/DEVLOG.md`, `.dev/BACKLOG.md`.**

**SPRINT 5.17 COMPLETE (2026-09-09): Gini Calibration Universe + Comparability Audit — methodology / empirical research sprint. pytest 373 green (unchanged — docs + read-only research only, no production code changes); model version stays `normalization-v0.6`; confidence stays None; no migration, no model-version bump. DEC-024 added (durable deferral decision). Two outputs: (1) Provider semantics + comparability audit — verified SI.POV.GINI (World Bank PIP): 0–100, higher = more inequality, mixes income-based (high-income: LIS/EU-SILC) and consumption-based (CHN/IND) surveys; the WB API does NOT expose a per-observation welfare-concept tag. (2) Calibration universe audit — read-only profile (`apps/api/scripts/gini_calibration_profile.py`) of tracked_8 (187 obs, 25.5–43.7) AND the broader WB global Gini universe (2430 country-year obs, 171 countries, 1963–2025, 20.2–71.1). Findings: tracked_8 is a NARROW non-representative subset (permanently rejected as calibration universe); global distribution shifts by decade (country count 2→160→115; decade medians 34–39); income-vs-consumption is a systematic level difference (IND consumption median 27.7 vs USA income median 40.8); no defensible level_score=50 interpretation exists; no external calibration basis for fixed thresholds. Verdict: **DEFER_GINI_LEVEL** — no defensible calibration universe or comparability basis exists yet; GINI_INDEX stays MONOTONIC_NEGATIVE (direction confirmed) with NO numeric curve; `100 - Gini` remains NOT approved; Wealth-gap force stays PARTIAL (DEC-009). Files changed: `apps/api/scripts/gini_calibration_profile.py` (NEW — read-only profile), `.dev/NORMALIZATION.md` (Sprint 5.17 section + decision matrix + verdict + §7 GINI_INDEX row update), `.dev/DECISIONS.md` (DEC-024), `.dev/HANDOFF.md`, `.dev/PROJECT_STATUS.md`, `.dev/DEVLOG.md`, `.dev/BACKLOG.md`, `.dev/DATA_SOURCES.md` (Gini welfare-concept clarification).**

**SPRINT 5.16 COMPLETE (2026-09-09): WGI Confidence Calibration Audit + Diagnostic Hardening — methodology / empirical research sprint. pytest 373 green (372 + 1 new connection_invalidated regression); model version stays `normalization-v0.6`; confidence stays None; no migration, no model-version bump. DEC-023 added (durable deferral decision). Two outputs: (1) Transaction hardening — `run_wgi_diagnostic_ingestion` now distinguishes recoverable `DBAPIError` (savepoint rolled back, FAILED IngestionRun retained, non-raising outcome) from `DBAPIError.connection_invalidated = True` (the DB connection is dead; the outer transaction CANNOT persist a FAILED audit row — the error PROPAGATES; no false audit-record promise). Focused regression for both cases. (2) WGI confidence calibration audit — a read-only empirical profile (`apps/api/scripts/wgi_confidence_profile.py`) of the 624 aligned score/diagnostic rows. Findings: CI width and SR are HIGHLY REDUNDANT (Pearson -0.83); dimension differences are substantial and unexplained (RL median width 9.23 / SR 12 vs PV median width 13.88 / SR 8.5); temporal trends make empirical calibration leaky or drifting (SR Pearson 0.543 vs year); boundary clipping is rare (9/624 = 1.4%, all CHE upper-bound); score vs width has essentially no pooled correlation (0.08). Verdict: **DEFER_NUMERIC_CONFIDENCE** — no defensible calibration basis exists yet; the diagnostics remain INPUT DATA only. Files changed: `app/services/wgi_diagnostic_ingestion.py` (connection_invalidated propagation), `tests/test_wgi_diagnostic_ingestion.py` (new regression), `apps/api/scripts/wgi_confidence_profile.py` (NEW — read-only profile), `.dev/NORMALIZATION.md` (Sprint 5.16 section + decision matrix + verdict + §17 item 6 update), `.dev/DECISIONS.md` (DEC-023), `.dev/HANDOFF.md`, `.dev/PROJECT_STATUS.md`, `.dev/DEVLOG.md`, `.dev/BACKLOG.md`.**

**SPRINT 5.15.1 COMPLETE (2026-09-09): Diagnostic ingestion failure-audit hardening — closes the one auditability gap in the Sprint 5.15 path. pytest 372 green (371 + 1 new DB-failure regression; the existing non-integer source-count test updated to assert the FAILED-run audit record); model version stays `normalization-v0.6`; confidence stays None; no migration, no model-version bump, no DEC change.** Fix: `run_wgi_diagnostic_ingestion` now wraps `persist_indicator_diagnostics` in a SAVEPOINT (`session.begin_nested()`). On an expected persistence/domain failure (`IndicatorDiagnosticPersistenceError`) or database failure (`DBAPIError`), the savepoint rolls back ONLY the diagnostic rows (including any half-added rows from a multi-DTO batch) while the outer IngestionRun row is retained, marked FAILED (completed_at / error_count=1 / structured errors[]), and returned as a non-raising outcome — the CLI commits the FAILED run exactly as it already does for fetch failures. Programmer bugs (anything outside the two caught classes) still propagate. No retry for persistence validation failures (the CLI's TransientRetryFetcher retries only transient network/5xx — unchanged). All existing behavior verified unchanged: successful imports, exact re-import skip, immutable revisions, fetch-failure → failed run, per-series isolation, WGI/DSR/credit-gap outputs, confidence None, normalization-v0.6. Files changed: `app/services/wgi_diagnostic_ingestion.py` (savepoint + catch), `tests/test_wgi_diagnostic_ingestion.py` (updated non-integer regression + new DB-failure regression), `.dev/PROJECT_STATUS.md` (living pytest count 318→372, Sprint 5.15 wording corrected: fetch + expected persistence failures leave audit records), `.dev/HANDOFF.md`, `.dev/DEVLOG.md`.

**SPRINT 5.15 COMPLETE (2026-09-09): WGI diagnostic LIVE INGESTION — the 9 owner-approved WGI uncertainty diagnostic series are now IMPORTED into `indicator_diagnostics`; NO confidence (input data only). pytest 371 green (340 + 31 new offline tests); model version stays `normalization-v0.6`; confidence stays None on every signal.** Built: (1) **dedicated fetch path** `app/data_sources/world_bank_wgi_diagnostics.py` — `WorldBankWgiDiagnosticFetcher.fetch_wgi_diagnostic(country_iso3, spec, start_year, end_year) -> list[IndicatorDiagnosticDTO]`, reusing the WB adapter's transport conventions (httpx, the cadata SSL workaround, timeout) but producing ONLY diagnostic DTOs — never an observation DTO, never a series row; requests the dedicated WGI source explicitly via `?source=3` on the plain v2 country/indicator endpoint (the `/v2/source/3/...` path form 404s — live-probed 2026-09-09, read-only) and VALIDATES the served provider identity on every response: `metadata[0].sourceid` must equal the expected dedicated-WGI source id (else SeriesMappingError — never retried, never silently adapted), each record's `indicator.id` must match the spec's exact series code and `countryiso3code` the requested country; period_start = YYYY-01-01 (annual, matching the score convention); raw_payload preserved per record; null values/records skipped (never 0); non-finite/bad numerics raise DataSourceParseError. (2) **ingestion service** `app/services/wgi_diagnostic_ingestion.py` — `run_wgi_diagnostic_ingestion` resolves the DataSource by source_key, records an IngestionRun with `data_kind="indicator_diagnostic"` + base_indicator_code / diagnostic_kind / provider_series_code / provider_source_code / year span in run_metadata, persists ONLY via `persist_indicator_diagnostics` (vintage 1 insert / skip identical / vintage N+1 on change, old vintages retained, caller-owned transaction); fetch failures are recorded as failed runs (committed by the CLI) rather than raised. (3) **separate batch CLI** `scripts/ingest_wgi_diagnostics.py` (--country/--all-countries, --indicator/--all-indicators, --kind/--all-kinds, --start/--end; full command `--all-countries --all-indicators --all-kinds --start 1996 --end 2025`) — deliberately NOT part of `ingest_world_bank.py --all-mapped`; transient-only retry (network/5xx, max 3 attempts; 4xx, parse, and provider/spec identity errors never retried); per-series failure isolation (each country × series in its own transaction; a failed series never rolls back successful ones). **Live import (dev Postgres): 72/72 series SUCCESS — 1872 rows = exactly the expected 8 countries × 3 WGI indicators × 3 kinds × 26 score years** (624 per kind, 624 per indicator, 234 per country; 1996–2024 with the biennial gaps 1997/1999/2001 only; no 2025), all vintage 1, 0 duplicate identities. Read-only validation: LB ≤ score ≤ UB for all 624/624 latest-vintage score points (0 violations); SR all finite/integer/non-negative (observed 4–16, descriptive — not encoded as thresholds); perfect one-to-one score-year alignment (no borrowed periods, no synthesized diagnostics); canonical isolation verified — Indicator 25 / SourceSeries 19 / Observation 5647 unchanged; idempotent re-run (CHE RL all 3 kinds): 78 received / 0 inserted / 78 skipped / 0 revised. Exact-period lookup smoke (CHE RULE_OF_LAW @2025-Q2 → score aligns to source year 2024): score 87.3184, LB 82.0371, UB 92.5997, CI width 10.5626 (DISPLAY ONLY — never attached to NormalizedSignal, never converted to confidence), SR 10; the 2025 diagnostic lookup returns None (no latest-available fallback, no year borrowing). 75 diagnostic IngestionRuns all success (72 first import + 3 idempotent re-runs). NO confidence formula, NO NormalizedSignal change, NO model-version bump, NO public API, NO frontend change; the diagnostics are confidence INPUT DATA only (§17 item 6 remains open). No DEC-023 — ordinary ingestion mechanics under DEC-022's existing representation.

**SPRINT 5.14 COMPLETE (2026-09-09): Indicator diagnostics STORAGE / PERSISTENCE / LOOKUP FOUNDATION (the accepted DEC-022 recommendation) — items 1 and 3 of the recorded spec plus the typed representations; the LIVE WB FETCH is deliberately NOT (that is Sprint 5.15). pytest 340 green (318 baseline + 22 new offline diagnostics tests); model version stays `normalization-v0.6`; confidence stays None on every signal; NOTHING INGESTED (the dev DB's indicator_diagnostics table has 0 rows); NO public API, NO frontend, NO NormalizedSignal change.** Built: (1) **`indicator_diagnostics` table** — Alembic `e3a7c94b1d51` (down_revision 38fff2cf97c9) APPLIED to the dev DB; observations 5647 / indicators 25 / source_series 19 verified IDENTICAL before and after; FKs to countries/indicators (the BASE canonical indicator — e.g. RULE_OF_LAW_WGI_SCORE)/data_sources; exactly 3 `IndicatorDiagnosticKind`s (ci_lower_bound / ci_upper_bound / source_count — NO SE, per DEC-022); `provider_source_code` (WB dedicated WGI source id "3", distinct from WDI source 2) + exact `provider_series_code` (GOV_WGI_RL.SC_LB etc.) + period_start + raw value + retrieved_at + immutable vintage_number + raw_payload; NO source_series_id, NO SourceSeries rows; DB-unique identity `uq_indicator_diagnostics_identity` (country, indicator, source, provider source, provider series, kind, period, vintage) + composite lookup index (country, indicator, kind, period). (2) **Immutable persistence** — `app/services/indicator_diagnostic_service.py`: `IndicatorDiagnosticDTO` (deliberately NOT an ObservationDTO) + `persist_indicator_diagnostics(session, dtos, specs)` — resolves country/base-indicator/DataSource, REJECTS unknown identities and any DTO whose provider identity does not match the expected spec, validates finite values and integer-valued non-negative source_count (9.7 is rejected, NEVER rounded to 10), compares against the latest vintage for the FULL identity, skips identical / inserts vintage N+1 on change, old vintages retained and never overwritten, no IndicatorRevision rows (the vintages are the history), caller-owned transaction (no commit inside); returns received/inserted/skipped/revised. (3) **WGI diagnostic spec registry** — `app/data_sources/wgi_diagnostic_specs.py`: 9 frozen specs (3 WGI indicators × 3 kinds) with the Sprint-5.13-verified series codes; provider-diagnostic metadata ONLY (seeds nothing — no canonical indicators, no SourceSeries); `validate_wgi_diagnostic_specs()` enforces 9 specs / no duplicate identities / one series per (indicator, kind) / explicit provider source. (4) **Exact-period lookup** — `get_indicator_diagnostics_for_period(session, country, base_indicator, period_start, expected_specs)`: latest-vintage values by kind at the EXACT aligned source period (same-country/same-dimension/same-period rule from DEC-022), country-scoped at every layer (ISSUE-004 class), NO prior-year fallback, NO future-year borrowing, NO today's-latest; missing → None (missing ≠ perfect ≠ zero; the base Observation stays fully usable); tied to the EXPECTED provider-series identity — if multiple provider identities claim one logical diagnostic it RAISES (IndicatorDiagnosticAmbiguityError), never resolves by row order. (5) **Catalog/coverage/normalization isolation regression-tested**: diagnostics never create Observation/SourceSeries/Indicator rows; /api/indicators, /api/countries indicator_count (still 25 canonical indicators), force coverage statuses, WGI/DSR/credit-gap normalization outputs, confidence None, backtest_safe False all verified unchanged. Methodology note added (NORMALIZATION.md): raw CI width (SC_UB − SC_LB) is NOT an unbiased precision measure everywhere (published bounds clip at the 0/100 score boundaries) and CI width + source count may overlap — future confidence composition must test for double-counting; this sprint stores RAW values only.

**SPRINT 5.13 COMPLETE (2026-09-09): Confidence layering + WGI uncertainty representation audit (DEC-022) — an ARCHITECTURE/METHODOLOGY sprint. NO production code changed; pytest 318 green unchanged; model version stays `normalization-v0.6`; confidence stays None on every signal; NOTHING ingested; NO migration executed.** Outcomes: (1) **Confidence layering resolved** — NORMALIZATION.md §10 rewritten into 10.1 permanent rules / 10.2 INDICATOR confidence / 10.3 FORCE confidence. Confidence is independent from strength (economic scores are NEVER multiplied by confidence — `level 90, confidence 0.4` is never collapsed to 36; `relative_score *= confidence` and `momentum *= confidence` prohibited); confidence is not freshness (the DEC-013 `freshness_factor` is the one freshness truth — CONSUMED at most, never recomputed, never equated); missing uncertainty metadata is neither perfect confidence (no default 1) nor zero — the measurement-uncertainty component stays UNKNOWN (None) and the score itself stays usable; NO arbitrary numeric provider-quality constants (qualitative categories may be TYPED later). Coverage completeness and DEC-009 proxy ceilings are FORCE-LAYER ONLY — a high-quality fresh POLITICAL_STABILITY_WGI_SCORE signal is never lowered because Internal conflict is PARTIAL-capped; a future ForceConfidence is NOT implemented. (2) **WGI uncertainty series VERIFIED live** (read-only probe; temporary script deleted): score `GOV_WGI_{RL,CC,PV}_SC` (WDI source 2) + dedicated WGI source (id 3) `GOV_WGI_{dim}.SC_LB`/`.SC_UB` (90% CI bounds FOR THE GOVERNANCE SCORE), `.SE` (standard error OF THE ESTIMATE), `.SR` (number of sources) — all 8 tracked countries × all 4 series × every score year 1996–2024 (208 obs/series; biennial 1997/1999/2001 gaps only; no 2025 yet), PERFECT one-to-one year alignment with the score series; LB ≤ score ≤ UB for all 624 tracked_8 points — the bounds are on the SAME 0–100 score scale as the imported score (symmetric, UB clamps at 100); SE is on the estimate scale (~0.15–0.25) and empirically width ≈ 56.5 × SE (NOT 65.8 × SE — SE cannot reconstruct the published bounds); SR integer counts 4–16. Sample 2024 CHE RL: score 87.32 / LB 82.04 / UB 92.60 / width 10.56 / SE 0.1871 / SR 10. (3) **Initial uncertainty input set chosen: LB + UB + SR** (CI width = UB − LB as the primary score-scale measurement diagnostic, SR as a distinct data-richness diagnostic; SE DEFERRED — estimate-scale, non-reconstructive). NO CI-width or source-count → confidence curve is designed or approved anywhere. (4) **Storage representation chosen: a dedicated `indicator_diagnostics` table associated with the BASE canonical indicator** (country, base indicator, diagnostic kind ci_lower_bound/ci_upper_bound/source_count, provider series code, period, value, retrieved-at, vintage; NO SourceSeries rows). Decision matrix: auxiliary canonical indicators REJECTED (catalog/API/indicator_count/force-coverage pollution); multiple SourceSeries per indicator INVALID (alignment selects by indicator — it could nondeterministically return a bound instead of the score); raw_payload metadata FACTUALLY UNAVAILABLE (WB score-series records carry no uncertainty fields). **The migration was deliberately STOPPED** — the minimal Sprint 5.14 spec is recorded in NORMALIZATION.md "Sprint 5.13" and DEC-022: (1) Alembic migration for indicator_diagnostics; (2) a WB fetch path for the SC_LB/.SC_UB/.SR series persisting ONLY into the diagnostics table (never `persist_observations`), idempotent + revision-aware, IngestionRun-recorded; (3) a derived-layer lookup helper — diagnostics at the ALIGNED source period (same country/dimension/period, latest vintage; missing → None); (4) nothing else. §17 item 6 is now open only for the numeric confidence composition/weights; the layering is settled.

**SPRINT 5.12 COMPLETE (2026-09-09): Credit-gap ONE_SIDED_VULNERABILITY level IMPLEMENTED (DEC-021) — the second non-WGI level signal.** The owner supplied the approved numeric curve for CREDIT_TO_GDP_GAP, implementing the Sprint 5.11/DEC-020 verdict: **level_score = 50 for every gap at or below +2pp** (including ALL negative gaps — no floor, no penalty; the DEC-020 negative-side retraction is preserved), **strictly linear from (+2, 50) to (+10, 0)** (slope −6.25 per pp), **0 at or above +10pp** (endpoint clamp). Verbatim owner table: −20 → 50.00, 0 → 50.00, +2 → 50.00, +4 → 37.50, +6 → 25.00, +9 → 6.25, +10 → 0.00, +20 → 0.00. **The no-excess region is deliberately NEUTRAL 50, NOT 100** — absence of excess credit is not evidence of strength; this resolves the DEC-020 open question in favor of "no-signal/neutral". All four parameters (neutral_ceiling 2.0, saturation_value 10.0, no_excess_score 50.0, saturated_score 0.0) are Atlas MODEL PARAMETERS versioned in `ModelVersionConfig.one_sided_vulnerability_configs`; the +2/+10 breakpoints COINCIDE with the Basel CCyB guide L/H reference points (bcbs187) but the 50/0 score mapping is an Atlas choice — the three threshold concepts separated in DEC-020 stay separated. **No minimum-history gate** (the curve is parametric — any single eligible observation scores, unlike the DSR own-history calibration). Freshness gates the current observation only and NEVER scales the score (quarterly class); too stale → no signal, never zero. Unchanged dimensions: relative stays CONTEXTUAL_DEFERRED (None), registry momentum windows (4q/8q) stay UNAPPROVED (momentum None), confidence None, backtest_safe False. **Execution gate** (same pattern as DSR): the registry one_sided_vulnerability family alone never auto-enables — an explicit model-version config entry is required, and v0.6 carries exactly one: CREDIT_TO_GDP_GAP. The DEC-020 Basel-caution score-interpretation limits (GDP-denominator distortions, trend turning points, structural breaks, endpoint revisions, post-bust artifacts — the gap is a common reference point, NOT a mechanical standalone rule) carry to every published use of this score. Model version **`normalization-v0.6`** / method `sprint-5.12-credit-gap-one-sided-level-r1`; all v0.5 WGI + DSR configuration unchanged (DSR live values verified unchanged: CHE @2025-Q4 → 13.4615). Live read-only smoke (tracked_8 @2025-Q4): USA −11.54 → 50, CHN −7.69 → 50, CHE −17.04 → 50, DEU −3.96 → 50, FRA −15.11 → 50, GBR −17.82 → 50, JPN +6.78 → **20.10**, IND +1.74 → 50 (7/8 in the neutral region — descriptive, not breakpoint validation). pytest **318 green** (312 + 6 net new: owner-table exact regression, pure-curve clamps/continuity/config-validation, no-auto-enable gate, no-observation → None, too-stale → None, freshness-decays-but-never-scales, other-dimensions-stay-None; the Sprint 5.11 still-unscored regression was replaced). No force aggregation/weights/phases (the credit-gap level now pairs with the DSR level as the Indebtedness force's second indicator-level input — force composition remains unapproved), no persistence, no public API, no frontend change, no migration.

**SPRINT 5.11 COMPLETE (2026-09-09): Credit-to-GDP gap evidence audit (DEC-020) — a METHODOLOGY sprint; NO score implemented.** Verdict: **RECLASSIFY / REFRAME — CREDIT_TO_GDP_GAP level family: asymmetric TARGET_BAND → ONE_SIDED_VULNERABILITY** (new `NormalizationFamily.one_sided_vulnerability` in the registry: no stress at or below a neutral ceiling, stress rises monotonically above it). Evidence (primary BIS/BCBS sources only): **Positive side SUPPORTED** — Basel CCyB guide L=+2pp / H=+10pp (BCBS 2010 bcbs187: linear 0→2.5%-of-RWA buffer add-on, flat zero below +2, explicitly non-mechanical) and BIS 2018 early-warning exercise (Aldasoro, Borio & Drehmann, BIS QR March 2018: critical threshold ~9pp standalone, amber 4–9 / red ≥9, ~4pp when combined with property prices) — two DIFFERENT instruments (policy guide vs statistical early warning), recorded separately, no fake consensus. **Negative side NOT supported — no threshold invented**: the Basel guide is flat zero below +2; the BIS 2014 Q&A (Drehmann & Tsatsaronis) treats the negative region as a "no consequence" post-crisis phase; the persistent negative gap after a boom-bust is a documented MEASUREMENT ARTIFACT (boom-contaminated HP trend understating renewed vulnerability, IMF WP 2020/006's assessment); post-crisis deleveraging is essentially uncorrelated with recovery pace (Takáts & Upper, BIS WP 416). The DEC-018 "very negative = deleveraging/weak credit" penalty expectation is RETRACTED — deleveraging/weak-credit conditions belong to OTHER signals (DSR own-history level, credit growth, output). Basel caution documented and carried forward: the gap is a "useful common reference point", judgment required, misleading-signal modes (GDP-denominator collapse, trend absorbing protracted booms, structural breaks, endpoint revisions, post-bust artifacts) must be score-interpretation limits on any future curve. Provider definition verified (credit/GDP − one-sided HP trend λ=400,000, total credit to private non-financial sector; Atlas consumes the published gap, never recomputes). Construction-vs-normalization history separated (the BIS "≥10 years" rule belongs to constructing the HP trend, not to consuming the published series — no Atlas minimum decided). BIS's one-sided filter does NOT solve Atlas release-date safety — backtest_safe stays False. Full evidence matrix + descriptive tracked_8 profile (832 obs: pooled buckets <0: 403, 0–2: 53, 2–4: 48, 4–9: 158, 9–10: 29, >10: 141 — DESCRIPTIVE ONLY, justify nothing) in NORMALIZATION.md "Sprint 5.11". Registry reclassified with an evidence note; CREDIT_TO_GDP_GAP still raises NormalizationNotImplementedError (regression-tested); NO model-version bump (normalization-v0.5 — no score semantics became executable); NO provisional numeric Atlas breakpoints recorded anywhere (neutral ceiling, curve shape, endpoints, no-excess-region interpretation = owner decisions for a future sprint). pytest **312 green** (311 + 1 still-unscored regression + updated exact-classification expectations). No force aggregation/weights/phases, no persistence, no public API, no frontend change, no migration, no new data imports.

**SPRINT 5.10 COMPLETE (2026-09-09): DSR OWN_HISTORY level signal (DEC-019) — the FIRST non-WGI level normalization.** For EXACTLY DEBT_SERVICE_RATIO, `level_score` answers "where is the country's current DSR relative to ITS OWN historical DSR distribution as of this scoring snapshot?" — NOT a cross-country comparison (cross-country raw-DSR ranking stays PROHIBITED per BIS caution), NOT a tracked_8/global percentile, NOT a force score, NOT a phase. **Method (all LOCKED in DEC-019)**: calibration = the country's EXPANDING own history of REAL observations from the first eligible observation through the current aligned observation, INCLUSIVE — selected by the new `alignment_service.own_history_as_of` (same semantics as `align_observation_as_of`: country isolation, indicator scoping, latest vintage per period, DEC-015 period-complete, no future data; no forward-fill/interpolation/zero-fill — MISSING ≠ ZERO). Empirical mid-rank plotting position: `stress_percentile = 100 * (average_rank − 0.5) / n`, rank 1 = lowest DSR = least stress, ties = average rank (deterministic, order-independent); **`level_score = 100 − stress_percentile`** (higher DSR = greater burden = weaker); endpoints NOT forced to 100/0 (n=20 lowest → 97.5), no clamping, no z-score/MAD/min-max/winsorization/external thresholds. **Minimum history = 20 observations — an ATLAS VERSIONED MODEL PARAMETER, NOT BIS methodology truth**; below 20 → level None (never 0, never stretched), the unscored signal still returns raw value + source period + freshness provenance + counts (`OwnHistoryLevelResult`: sample_n, minimum_sample_n, earliest/latest_source_period, rank, stress_percentile, level_score on `NormalizedSignal.own_history_level`). Freshness gates the CURRENT observation only and NEVER scales the score; historical calibration points are never decayed. **Execution gate**: OWN_HISTORY level requires BOTH the registry level family AND an explicit `own_history_level_configs` entry in the model version (v0.5 carries exactly one: DEBT_SERVICE_RATIO min 20) — a registry entry alone never auto-enables; no generic fallback. DSR relative = None (prohibited by design), DSR momentum NOT implemented (registry 4q/8q windows stay unapproved), confidence None, backtest_safe False. `normalize_indicator_as_of` refactored into a dispatcher: DIRECT_0_100 → the WGI path UNCHANGED (byte-equivalent, regression-tested), OWN_HISTORY → the DSR path, all other families raise NormalizationNotImplementedError. Model version **`normalization-v0.5`** / method `sprint-5.10-dsr-own-history-level-r1`; all WGI v0.3/v0.4 config unchanged. Live read-only smoke (Postgres, nothing written): all tracked_8 @2025-Q4 score with n=104 histories — USA raw 14.1 → level 94.7115 (rank 6/104), CHN raw 18.8 → level 2.88462 (rank 101.5/104), CHE raw 19.7 → 13.4615; boundary verified: CHE @2004-Q3 → raw 15.6 present, n=19, level None (not zero), CHE @2004-Q4 → n=20, first score 97.5 (rank 1, stress 2.5). pytest **311 green** (285 + 26 new DSR tests in tests/test_dsr_own_history.py: leakage/future-exclusion, latest-vintage-in-sample, country isolation, no zero-fill, n=19→None / n=20→score, ties, endpoints, exact formula, stale→no-signal, freshness never scales, dimensions stay None, WGI unchanged, no writes, no force fields). No persistence, no public API, no force aggregation/weights/phases, no frontend change, no migration.

**SPRINT 5.9 COMPLETE (2026-09-09): normalization parameter decision audit + relative-path hardening (DEC-018) — a METHODOLOGY sprint, no new scores published.** Two outputs: (1) **Hardening** — `build_relative_cross_section` (apps/api/app/cycle/relative.py) now enforces its OWN execution gate: level family DIRECT_0_100 AND relative family CROSS_SECTIONAL_RELATIVE (exactly the WGI ×3 under the current registry; no WGI codes hardcoded) — a direct helper call for GINI_INDEX / GDP_GROWTH / GROSS_CAPITAL_FORMATION_GDP / LABOUR_PRODUCTIVITY_PER_HOUR / INFLATION_CPI or any other non-approved indicator now raises `NormalizationNotImplementedError` instead of possibly ranking it. Complete-universe / freshness / alignment rules unchanged; WGI numerical outputs verified unchanged (live read-only smoke: CHE RL @2025-Q2 → 87.3184 / relative 93.75 rank 8/8 / momentum −2.58971; CHN RL → 6.25 rank 1/8); NO model-version bump (no output semantics changed). (2) **Non-WGI level audit (DEC-018)** — the eight proposed non-WGI level methodologies were critically reviewed before any numeric thresholds were implemented; NO thresholds were invented. RECLASSIFIED to CONTEXTUAL_DEFERRED (level): GDP_GROWTH (universal band would punish catch-up growth — tracked_8 GDP-growth medians 2015–2025 span 0.8–7.2), GROSS_CAPITAL_FORMATION_GDP (MONOTONIC_SATURATING disproved — very high GCF can be credit-driven overinvestment; healthy level is economy-model-dependent: medians CHN 42.4 vs GBR 18.5), INFLATION_CPI (objectives differ across regimes: medians IND 6.35 vs JPN 0.29 — a universal band would encode "2% ideal for every country"), UNIT_LABOUR_COST_GROWTH (raw domestic ULC growth is not a relative-competitiveness measure; no FX/partner-ULC context). CONFIRMED families: GINI_INDEX MONOTONIC_NEGATIVE direction (but "100 − Gini" NOT approved — no numeric curve), CREDIT_TO_GDP_GAP asymmetric TARGET_BAND (thresholds UNRESOLVED; BIS early-warning research question recorded), DEBT_SERVICE_RATIO OWN_HISTORY — **READY_FOR_IMPLEMENTATION_DESIGN, selected as the NEXT implementation target** (self-contained own-history design needs no external calibration; 104 quarters/country), LABOUR_PRODUCTIVITY_PER_HOUR MONOTONIC_POSITIVE direction (absolute level curve deferred — tracked_8 min-max explicitly rejected). New read-only research script `apps/api/scripts/normalization_profile.py` (descriptive per-country + pooled stats for the audited indicators — NO scores, NO writes, NOT proof that an empirical percentile is economic truth). Doc drift fixed: stale Sprint-5.5/5.6 "nothing computed" wording, freshness shape now documented as SETTLED exponential (DEC-013; linear kept as an available alternative), WGI biennial-gap status resolved-for-current-path, HANDOFF DEC-005/DEC-006 summary labels corrected (DECISIONS.md is authoritative), "other 15/16 indicators" → "non-WGI indicators". pytest **285 green** (278 + 5 hardening + 2 registry-classification regressions). backtest_safe stays False; no force aggregation/weights/phases, no persistence, no public API, no frontend change.

**SPRINT 5.8 COMPLETE (2026-09-09): WGI CROSS_SECTIONAL_RELATIVE scores on tracked_8 (DEC-017) — the third executable score dimension.** `relative_score` = the country's **relative position within the fixed, versioned tracked_8 universe** (USA, CHN, CHE, DEU, FRA, GBR, JPN, IND — a typed frozen `ReferenceUniverseSpec`, NEVER derived from DB contents; a changed universe = new id + new model version). Formula: **mid-rank plotting position** `100 * (average_rank − 0.5) / n`, rank 1 = weakest, rank n = strongest, **ties = average rank** (deterministic, order-independent — never broken by ISO code/row id/query order), higher WGI = stronger, no inversion. n=8 scores are 6.25…93.75 — never mislabeled 0/100. **NOT a global/world percentile, NOT a level, NOT a phase**; level_score keeps its own meaning untouched. Every member is aligned with `align_observation_as_of` at the SAME scoring snapshot (country isolation ISSUE-004, latest vintage, DEC-015 period-complete eligibility, no-future-leakage all inherited; never "latest overall"). **Complete-universe rule**: all 8 members must be usable, else NOBODY is scored (7/8 → None for everyone, never a seven-country score called tracked_8) with `reference_universe_id` / `reference_universe_expected_n` / `reference_universe_usable_n` / `relative_rank` provenance carried; missing members never become zero. Freshness gates member USABILITY only — it NEVER scales relative_score; confidence stays None. Participating values are validated 0..100 (out-of-range raises NormalizationDataError — never clamps/ranks/treats-as-missing). Execution gate: relative_family CROSS_SECTIONAL_RELATIVE AND level DIRECT_0_100 → exactly the WGI ×3 (GDP_GROWTH / GCF / Gini / labour-productivity registry CROSS_SECTIONAL_RELATIVE entries do NOT enable — they still raise NormalizationNotImplementedError). Relative is independent from momentum: either may exist when the other is None. Model version **`normalization-v0.4`** / method `sprint-5.8-wgi-tracked8-relative-r1` (versioning universe id + frozen membership, complete-universe rule, mid-rank formula, tie method, WGI direction, the v0.3 momentum config, exponential freshness policy); backtest_safe stays False. Live read-only smoke (live Postgres, nothing written, nothing hardcoded): CHE RULE_OF_LAW @2025-Q2 → level 87.3184, relative **93.75** (rank 8/8, universe 8/8), momentum −2.58971 unchanged; CHN RULE_OF_LAW @2025-Q2 → relative **6.25** (rank 1/8); USA CONTROL_OF_CORRUPTION @2025-Q2 → relative **31.25** (rank 3/8). pytest 278 green (243 + 35 new relative regressions in tests/test_relative_scores.py). NO relative for non-WGI indicators, NO confidence, NO force aggregation/weights/phases, NO persistence, NO public API, NO frontend change.

**SPRINT 5.7 COMPLETE (2026-09-09): WGI OWN_HISTORY momentum (DEC-016) — the second executable score dimension, on the Part 0-hardened period-complete alignment path.** Momentum = the SIGNED change in the provider's own 0–100 points: `momentum = current_aligned_raw − anchor_aligned_raw`, computed for exactly the WGI ×3 (RULE_OF_LAW / CONTROL_OF_CORRUPTION / POLITICAL_STABILITY_WGI_SCORE; gate = level_family DIRECT_0_100 AND momentum_family OWN_HISTORY — other indicators' registry OWN_HISTORY entries do NOT approve their momentum, no generic fallback). Windows (3y, 5y) both computed; **headline momentum = the 5y primary window ONLY** (no averaging, no silent fallback to 3y); all windows travel as `MomentumWindowResult` provenance (window_years, requested_anchor_period, anchor_source_period, change). Anchors come from `shift_scoring_period_years` (quarter preserved) + a plain `align_observation_as_of` call at the shifted period — country isolation, latest vintage, DEC-015 period-complete eligibility, and no-future-leakage are all inherited, no separate momentum query logic. **Anchor tolerance = 1 annual period** (versioned model parameter, owner-LOCKED for 5.7): the aligned anchor may be at most one source year older than the requested anchor year (covers the WGI 1997/1999/2001 biennial gaps); outside tolerance → change None — never zero, never a stretched window, the actual anchor stays visible. Higher WGI = positive momentum, no inversion; computed from aligned RAW values, never level_score. Freshness gates the CURRENT observation only — the anchor is never decayed and momentum is never multiplied by freshness. Missing history → momentum None, never 0; the level signal is still produced. Model version **`normalization-v0.3`** / method `sprint-5.7-wgi-momentum-r1` (versioning windows/primary/tolerance/sign/freshness policy); backtest_safe stays False. Live read-only smoke (all on live Postgres, nothing written): CHE RULE_OF_LAW @2025-Q2 → level 87.3184, momentum −2.58971 (5y, anchor 2019; 3y −0.443138, anchor 2021); CHE @2001-Q2 → level 91.4538 with momentum None (no alignable 5y anchor; the 3y anchor aligned 1996 is outside tolerance — no fallback, no zero); CHN CONTROL_OF_CORRUPTION @2025-Q2 → +2.79934; DEU POLITICAL_STABILITY @2025-Q2 → −10.2986; USA CONTROL_OF_CORRUPTION @2025-Q2 → −1.72217. WGI momentum is indicator-specific and NOT approved for direct cross-indicator aggregation. pytest 243 green (225 + 18 new momentum regressions). NO relative scores, NO other-indicator momentum, NO force aggregation, NO persistence, NO public API, NO frontend change. Part 0 (period-complete alignment, DEC-015) closed immediately before this; Sprint 5.6 (first executable path) and Sprint 5.5 (normalization design, DEC-012) before that.

**PART 0 COMPLETE (2026-09-09): period-complete alignment hardening (DEC-015), applied BEFORE any momentum work.** The Sprint 5.6 eligibility rule (`period_start <= as_of_date`) admitted period-INCOMPLETE observations — an annual 2020 observation was eligible at the 2020-Q2 snapshot even though the 2020 period has not ended. Eligibility is now **`effective_period_end <= scoring_period_end`** in the DERIVED layer only: annual/irregular YYYY → YYYY-12-31, quarterly YYYY-Qn → the quarter's end day (Q1→03-31, Q2→06-30, Q3→09-30, Q4→12-31). `effective_period_end` travels as `AlignedValue` provenance; raw observations are never modified, no synthetic period-end dates persisted. Implementation: `apps/api/app/services/alignment_service.py` (`effective_period_end` helper + an index-based SQL eligibility expression that renders identically on SQLite and PostgreSQL), `apps/api/app/cycle/normalization_definitions.py` (AlignedValue + `effective_period_end` field; alignment semantics changed → CURRENT_MODEL_VERSION bumped `normalization-v0.1` → **`normalization-v0.2`**, method `part0-period-complete-alignment-r1`). Live-DB verification (read-only smoke): CHE RULE_OF_LAW_WGI_SCORE @2024-Q2 now aligns to **2023** (87.8079 — was the incomplete-year 2024 value: the leak), first becomes 2024-eligible at 2024-Q4 (87.3184, freshness 1.0000); @2025-Q2 unchanged (2024, 87.3184, freshness 0.9170); BIS CREDIT_TO_GDP_GAP aligns to exactly its own completed quarter (2025-Q1/Q2/Q3 verified on live Postgres). **This fixes period-completeness leakage ONLY — NO historical release-date safety; every signal keeps `backtest_safe = False` until Milestone 9.** pytest 225 green (220 + 5 new period-completeness regressions in tests/test_normalization_signals.py; the former leak expectation inverted). NO momentum, NO other families, NO force aggregation, NO frontend change, NO new data, NO migrations, NO raw-layer change. Sprint 5.6 (first executable path: alignment + freshness + DIRECT_0_100 for the WGI ×3) and Sprint 5.5 (normalization design, DEC-012) complete before that; Milestones 4, 5.0–5.4 before that. Owner decisions recorded 2026-09-09: DEC-013 (freshness decay shape = exponential, confirmed) and DEC-014 (Sprint 5.7 direction = WGI momentum; relative scores + TARGET_BAND design deferred).
- FastAPI backend: `/health`, `/api/countries` (with data coverage fields), `/api/countries/{iso3}`, `/api/countries/{iso3}/observations` (latest vintage only; `?indicator=`, `?source=`, `?start=`, `?end=` filters; `source_key` provenance on every row), `/api/countries/{iso3}/force-coverage` (17 forces, statuses only), `/api/indicators`, `/api/data/sources`, `/api/data/ingestion-runs`.
- Next.js frontend: home page (cards with provider-neutral coverage), `/countries` (coverage column), `/country/[iso3]` with Economic data (WB) + Trade & Investment (WB, 5.1) + Governance (WB WGI, 5.2) + Inequality (WB Gini, 5.3) + **Military (WB-republished SIPRI, new in 5.4)** + Debt & credit (BIS) + Productivity & Competitiveness (OECD; CHN/IND safe empty state) + Big Cycle Forces entry card, `/country/[iso3]/forces` (all 17 forces with data-coverage statuses and a dynamic summary — no gauges, no score colors, no Rise/Peak/Decline).
- Live data: 5647 observations — WB 2953 (3 GDP indicators 264 + 5 trade/investment 1031 + 3 WGI governance scores 624 + GINI_INDEX 187 + INFLATION_CPI 287 + 2 military series 560, all 8 countries) + BIS 1664 (2000 Q1–2025 Q4) + OECD 1030 (1990–2025 request range). Idempotent re-import verified for the military series (CHE: 0 inserted / 35+35 skipped / 0 revised; 0 duplicate identity groups).
- Canonical catalog is 25 indicators (23 + MILITARY_EXPENDITURE_USD + MILITARY_EXPENDITURE_GDP added in 5.4); 19 SourceSeries seeded (15 WB + 2 BIS + 2 OECD).

## Completed

- Milestone 1: monorepo, FastAPI app, Next.js frontend, countries table + seed, docker-compose, README
- Milestone 2: full data model (indicators, observations, data sources, ingestion runs, source series) with Alembic migrations, services, routes, pagination
- Milestone 3: World Bank connector — adapter, central mapping registry, ingestion service with caller-owned transactions, revision handling (immutable vintages + IndicatorRevision), batch CLI for all countries, coverage API + UI
- Milestone 4 (done so far): BIS end-to-end + OECD end-to-end (see DATA_SOURCES.md)
- Milestone 5.0: 17-force coverage foundation — `app/cycle/force_definitions.py` (17 forces, wording preserved from docs/data-model.md + packages/shared; live/candidate indicator mapping; no weights/scores), `app/services/force_coverage_service.py` (deterministic statuses, 3 aggregate queries, no N+1), `GET /api/countries/{iso3}/force-coverage`, `/country/[iso3]/forces` page + country-page entry card, `.dev/FORCE_COVERAGE.md` gap report
- Milestone 5.1 (2026-09-08): WB trade & investment expansion — 5 verified mappings (EXPORTS_GDP→NE.EXP.GNFS.ZS, IMPORTS_GDP→NE.IMP.GNFS.ZS, TRADE_BALANCE→NE.RSB.GNFS.ZS as the *published* external-balance series — NOT derived, CURRENT_ACCOUNT_GDP→BN.CAB.XOKA.GD.ZS, GROSS_CAPITAL_FORMATION_GDP→NE.GDI.TOTL.ZS), 1031 observations imported for all 8 countries 2000–2025 (40/40 series success), forces promoted in config: Trade and capital flows (4 live inputs) + Infrastructure and investment (GCF) → available with data; Global openness deliberately kept missing (trade is a prerequisite, not the whole force — note updated); education (SE.TER.ENRR / SE.SEC.ENRR are *gross* enrollment ratios ≠ canonical age-specific wording) and government debt (GC.DOD.TOTL.GD.ZS is *central* government ≠ canonical general-government concept) audited and NOT persisted (owner decisions pending); /country/[iso3] gained a compact Trade & Investment section
- Milestone 5.2 (2026-09-08): WGI governance expansion — runtime route diagnosis (no code bug: the live :8000 process predates the M5.0 route; a fresh import serves it — note FastAPI 0.141.1 keeps included routers as lazy `_IncludedRouter` wrappers, so inspect openapi.json rather than raw `app.routes` paths), 3 current WDI 2025-revision WGI score series verified against official WB metadata (legacy RL.EST/CC.EST/PV.EST no longer exist in WDI; WGI publishes SE / SC_LB / SC_UB / SR uncertainty series — documented, not imported), 3 mappings + 3 canonical indicators (Governance category, positive direction) seeded idempotently, 624 observations imported for all 8 countries 1996–2024 (24/24 series success), forces promoted: Rule of law (RULE_OF_LAW_WGI_SCORE), Corruption (CONTROL_OF_CORRUPTION_WGI_SCORE — higher = stronger control / less corruption, raw value never reversed), Internal conflict (POLITICAL_STABILITY_WGI_SCORE — institutional/conflict-risk proxy, does not measure every form of domestic conflict); Governance section on /country/[iso3] with perception/uncertainty caption; education + government-debt audit outcomes recorded as owner decisions (DEC-007 Option C; DEC-008 keep general-government)
- Milestone 5.3 (2026-09-08): inequality + CPI + coverage-semantics hardening — live route root cause (orphaned uvicorn reload worker, see Known issues) + restart + verification; coverage-ceiling mechanism (DEC-009: optional `coverage_ceiling` on ForceDefinition caps a proxy-only force at PARTIAL even with complete data; applied to Wealth/opportunity/values gaps + Internal conflict — internal conflict AVAILABLE → PARTIAL is a methodology correction, not a regression); WB Gini verified (SI.POV.GINI, 0–100 index, irregular publication, raw values stored exactly as published, never rescaled/forward-filled, missing years are gaps) + GINI_INDEX canonical entry (Inequality category, strength negative) + 187 obs imported 8/8 countries; WB CPI verified (FP.CPI.TOTL.ZG, annual %) + INFLATION_CPI mapped (no new canonical — frequency corrected monthly → annual per DEC-010, documented not silent) + 287 obs imported; forces: wealth_opportunity_values_gaps live = GINI_INDEX capped PARTIAL (note names missing wealth/opportunity/values concepts + future WID/ACLED-style inputs), cost_competitiveness live = UNIT_LABOUR_COST_GROWTH + INFLATION_CPI (OECD-6 AVAILABLE; CHN/IND PARTIAL from CPI alone — CPI is domestic price pressure, never a relative-competitiveness measure by itself); Inequality UI section with "Latest available: {year}" staleness label + income-inequality-only caption
- Sprint 5.7 (2026-09-09): WGI momentum (DEC-016) — OWN_HISTORY signed raw-point change for the WGI ×3; windows (3, 5) with 5y primary; anchor tolerance 1 annual period (versioned); anchors via align_observation_as_of at the shifted period (DEC-015 inherited); MomentumWindowResult provenance on every signal; missing history → None never 0; anchor never freshness-decayed; out-of-range raises never clamps; model version normalization-v0.3 / sprint-5.7-wgi-momentum-r1; 18 new offline tests (243 total); extended read-only smoke script
- Sprint 5.8 (2026-09-09): WGI relative scores (DEC-017) — CROSS_SECTIONAL_RELATIVE mid-rank plotting position (100 * (avg_rank − 0.5) / n, ties = average rank, higher WGI = stronger) for the WGI ×3 within the frozen typed tracked_8 ReferenceUniverseSpec; every member aligned at the SAME as-of snapshot (DEC-015 inherited); complete-universe rule (8/8 or nobody, usable/expected counts as provenance, missing never zero); freshness gates usability only, never scales the score; relative independent from momentum; out-of-range members raise; model version normalization-v0.4 / sprint-5.8-wgi-tracked8-relative-r1; 35 new offline tests (278 total); extended read-only smoke script; NOT a global percentile
- Sprint 5.10 (2026-09-09): DSR OWN_HISTORY level (DEC-019) — first non-WGI level signal: expanding as-of own-history calibration (real observations only, latest vintage, DEC-015, no future data), empirical mid-rank stress percentile (ties = average rank), level = 100 − stress_percentile, minimum 20 observations (Atlas MODEL PARAMETER; below → None never 0), endpoints not forced, freshness gates current only and never scales, OwnHistoryLevelResult provenance, explicit model-config execution gate (no registry auto-enable); model version normalization-v0.5 / sprint-5.10-dsr-own-history-level-r1; 26 new offline tests (311 total); --all-tracked smoke mode; DSR relative prohibited / momentum not implemented / confidence None
- Sprint 5.11 (2026-09-09): Credit-to-GDP gap evidence audit (DEC-020) — methodology only, no score: positive-side breakpoints SUPPORTED by official evidence (Basel CCyB guide +2/+10 as a policy reference point; BIS 2018 EWI red ~9 / amber 4–9), negative-side penalty NOT supported (guide flat zero below +2; negative region = post-crisis phase and/or boom-contaminated-trend artifact); family reclassified asymmetric TARGET_BAND → ONE_SIDED_VULNERABILITY (new enum member + registry note); three threshold concepts kept separated (policy guide vs statistical early-warning vs Atlas curve parameters); provider definition verified; construction-vs-normalization history separated; Basel judgment/misleading-signal caution documented; credit gap still raises NormalizationNotImplementedError (regression-tested); no model-version bump (v0.5); 312 total; NO provisional numeric breakpoints recorded
- Sprint 5.12 (2026-09-09): Credit-gap ONE_SIDED_VULNERABILITY level (DEC-021) — second non-WGI level signal, for exactly CREDIT_TO_GDP_GAP: owner-approved curve (flat 50 at/below +2pp, linear to 0 at +10pp, clamped above; no-excess region = neutral 50, never 100; negative gaps carry no penalty), versioned as OneSidedVulnerabilityConfig model parameters with OneSidedVulnerabilityResult provenance, execution gated by the explicit model-config entry (no registry auto-enable), no minimum-history gate (parametric), freshness gates current only and never scales, Basel-caution score-interpretation limits carried; model version normalization-v0.6 / sprint-5.12-credit-gap-one-sided-level-r1; 6 net new offline tests (318 total); relative stays deferred, momentum windows (4q/8q) stay unapproved, confidence None
- Sprint 5.13 (2026-09-09): Confidence layering + WGI uncertainty audit (DEC-022) — architecture only, NO code change, pytest 318 unchanged, model version stays normalization-v0.6, confidence None, nothing ingested: indicator confidence separated from force confidence (coverage/proxy ceilings force-layer only; confidence never multiplies scores; confidence ≠ freshness; missing uncertainty ≠ perfect ≠ zero; no arbitrary provider-quality constants); WGI uncertainty series verified live (LB/UB on the 0–100 score scale bracketing the score, SE estimate-scale and non-reconstructive (width ≈ 56.5×SE), SR integer counts, perfect tracked_8 one-to-one alignment 1996–2024); input set LB+UB+SR chosen (SE deferred); representation = dedicated indicator_diagnostics table (migration deliberately STOPPED — Sprint 5.14 spec recorded)
- Sprint 5.14 (2026-09-09): Indicator diagnostics storage/persistence/lookup foundation (DEC-022 implementation) — indicator_diagnostics table (Alembic e3a7c94b1d51 APPLIED to the dev DB; counts verified unchanged; 0 rows), IndicatorDiagnosticKind (exactly 3 kinds), immutable idempotent revision-aware persistence (persist_indicator_diagnostics + IndicatorDiagnosticDTO, structurally separate from persist_observations), 9-spec WGI diagnostic registry (app/data_sources/wgi_diagnostic_specs.py, WB WGI source id 3), exact-period country-scoped lookup helper (no fallback, missing → None, loud ambiguity errors), 22 offline isolation/persistence/lookup tests (340 total); live WB ingestion of the uncertainty series deliberately NOT built (Sprint 5.15); nothing ingested, no version bump, no public API
- Sprint 5.15 (2026-09-09): WGI diagnostic live ingestion — dedicated source-3 fetch path (WorldBankWgiDiagnosticFetcher → IndicatorDiagnosticDTO, provider identity validated on every response: metadata sourceid / record indicator.id / countryiso3code; never the canonical observation path), ingestion service (IngestionRun data_kind="indicator_diagnostic", persist_indicator_diagnostics only), separate batch CLI scripts/ingest_wgi_diagnostics.py (transient-only retry, per-series failure isolation); live import 72/72 series success — 1872 rows = 8 countries × 3 indicators × 3 kinds × 26 score years (all vintage 1, 0 duplicates; 624/kind, 624/indicator, 234/country; 1996–2024 with the 1997/1999/2001 gaps), LB ≤ score ≤ UB 624/624, SR integer 4–16, one-to-one score-year alignment, canonical 25/19/5647 unchanged, idempotent re-run 0/78/0, exact-period lookup smoke with CI width display-only; 31 new offline tests (371 total); no confidence, no version bump (v0.6), no API, no frontend
- Milestone 5.4 (2026-09-08): military-expenditure proxy + normalization-readiness audit — the 2 SIPRI-derived WDI series verified against official WB API metadata (MS.MIL.XPND.CD / MS.MIL.XPND.GD.ZS, annual, sourceOrganization = SIPRI Military Expenditure Database; WB open-data licensing defaults to CC-BY 4.0 — attribution recorded in DATA_SOURCES.md, documentation not legal advice); no `app/data_sources/sipri.py`, no SIPRI workbook download/rehost — source_key stays `world_bank`; 2 canonical indicators (Military category, annual, strength_direction contextual — more spending ≠ more capability) + 2 mappings seeded idempotently (WB mappings 13→15, catalog 23→25, SourceSeries 17→19); 560 obs imported for all 8 countries (complete 35-year histories 1990–2024, 16/16 series success; idempotent CHE re-runs 0/35/0 both indicators; 0 duplicate identity groups); Military strength promoted to live = MILITARY_EXPENDITURE_USD + MILITARY_EXPENDITURE_GDP with `coverage_ceiling = partial` (DEC-011 via DEC-009 — SIPRI describes expenditure as an INPUT measure; personnel/equipment/technology/logistics/readiness/experience/alliances/nuclear are unmeasured; MISSING → PARTIAL, never AVAILABLE from spending); no spending normalization (raw observations only); Military UI section (compact $ + % of GDP, year, "Source: World Bank · Underlying source: SIPRI", input-proxy caption, no history/ranking/score); "Normalization Readiness" audit in .dev/FORCE_COVERAGE.md (all 19 live series: direction/frequency/source/comparability/transformation/freshness/ceiling impact + 7 findings for 5.5 — audit only, no formulas)
- Sprint 5.5 (2026-09-09): normalization methodology & architecture design — `.dev/NORMALIZATION.md` (score semantics, quarterly clock, as-of alignment, backtest-safety limitation, freshness policy, normalization families, per-indicator classification for all 19 live series, absolute-vs-relative, momentum, confidence architecture, missing-data rules, ceiling behavior, versioning, reference universe/windows, robust statistics, force-score eligibility, 11 unresolved questions) + `apps/api/app/cycle/normalization_definitions.py` (typed skeleton + registry; validates at import) + `apps/api/tests/test_normalization.py` (19 offline tests) + DEC-012. Design only — nothing computed, nothing persisted, no API/frontend change
- Sprint 5.6 (2026-09-09): first executable normalization path — as-of alignment service (quarter-end semantics, latest vintage, latest prior period, country isolation), freshness machinery (parameterized decay shape, exponential initial default, stale → unusable → no signal), DIRECT_0_100 normalizer for the WGI ×3 (level_score = validated raw 0–100 value; out-of-range raises, never clamps), all non-WGI indicators raise NormalizationNotImplementedError, NormalizedSignal gained freshness_factor/is_stale provenance fields (relative/momentum/confidence/reference_universe_id stay None), CURRENT_MODEL_VERSION bumped to `normalization-v0.1` / `sprint-5.6-direct-0-100-r1`, read-only smoke script, 27 offline tests. On-demand computation only — nothing persisted, no public API
- Data coverage surfaced on GET /api/countries (observation_count, indicator_count, latest_observation_year — single aggregate query, latest vintages only) and on /countries + homepage cards; updates generically per source
- Batch importers: `--all-countries` (tracked list from seeded Country table), transient-retry wrapper (max 3 attempts; retries network/5xx only — never 4xx, parse, mapping, or no-data), failure isolation (one failed series never aborts the batch), end-of-batch summary; OECD batch adds a 1-second inter-request delay (documented ~60 queries/hour)
- Observation identity unique constraint `uq_observations_identity` (country, source_series, period_start, vintage_number) — concurrent ingestion runs can no longer insert duplicate vintages

## In progress

- Nothing mid-flight. Sprint 5.17 closed 2026-09-09 (Gini calibration universe + comparability audit: read-only profile of tracked_8 187 obs + WB global 2430 obs/171 countries -> tracked_8 rejected as calibration universe, global distribution shifts by decade, income-vs-consumption comparability insufficient, no defensible midpoint -> verdict DEFER_GINI_LEVEL, DEC-024. pytest 373 green unchanged). Sprint 5.16 closed 2026-09-09 (transaction hardening: recoverable DBAPIError -> FAILED run; connection_invalidated DBAPIError -> propagate, no false audit promise; 1 new regression. WGI confidence calibration audit: read-only profile of 624 rows -> CI width/SR redundancy Pearson -0.83, dimension differences substantial, temporal trends leaky/difting, no external calibration basis -> verdict DEFER_NUMERIC_CONFIDENCE, DEC-023. pytest 373 green). Sprint 5.15.1 closed 2026-09-09 (diagnostic ingestion failure-audit hardening: SAVEPOINT + catch persistence/DB failures -> FAILED run). Sprint 5.15 closed 2026-09-09 (read-only live WB probe confirming the `?source=3` mechanism + metadata sourceid validation → WorldBankWgiDiagnosticFetcher → wgi_diagnostic_ingestion service → scripts/ingest_wgi_diagnostics.py CLI → live import 72/72 series (1872 rows) → read-only post-import validation + idempotency re-run + exact-period lookup smoke → docs). No dev servers were needed (no frontend change); restart them in your own terminals when browser work resumes (`uv run --no-sync python run.py` from apps/api; `npm run dev -w apps/web` from the repo root).

## Next task

**Sprint 5.18 — owner to choose.** Two methodology deferrals are now in place: WGI numeric confidence (DEC-023) and Gini numeric level (DEC-024). Both require external evidence / metadata / methodology decisions before they can be defensibly implemented (see the DEC-023 and DEC-024 "what additional evidence is required" lists). Other candidates: **productivity expanded calibration universe** (OECD-wide distribution decision); **data-side work** — education Option C (DEC-007), IMF WEO general-government debt (DEC-008), WID wealth shares (lifts the Wealth-gap PARTIAL ceiling — though a Gini level alone would NOT lift it, per DEC-009/DEC-024). Deliberately NOT queued: DSR momentum (registry 4q/8q windows unapproved), credit-gap momentum (registry 4q/8q windows unapproved), non-WGI relative (cross-country raw-DSR ranking prohibited; credit-gap relative stays CONTEXTUAL_DEFERRED).

## Current architecture

Monorepo. `apps/api` is FastAPI + SQLAlchemy async + Pydantic, layering api/ → services/ → db/. `apps/web` is Next.js App Router with a typed API client in lib/. `packages/shared` holds shared country constants (single source of truth for ISO3 codes). PostgreSQL via docker-compose or host service.

## Running locally

Frontend:
npm run dev (in apps/web) — or `npm run dev -w apps/web` from root

Backend:
uv run python run.py (in apps/api)

Database:
PostgreSQL running on host (port 5432) or `docker compose up -d`

## Current routes

- / — global dashboard placeholder (headline + phase summary intro), country cards show data coverage
- /countries — country table from API with data coverage column
- /country/[iso3] — country detail with Economic data (all 8 countries) + Trade & Investment (all 8: latest exports/imports/trade balance/current account/gross capital formation, % of GDP; 5.1) + Governance (all 8: latest WGI Rule of Law / Control of Corruption / Political Stability, XX.XX / 100, year, World Bank / WGI source, perception/uncertainty caption; 5.2) + Inequality (all 8: latest Gini index with "Latest available: {year}" staleness label and the income-inequality-only caption; 5.3) + Military (all 8: latest military expenditure in compact current US$ and % of GDP, year, "Source: World Bank · Underlying source: SIPRI", input-proxy caption "spending alone does not measure military capability"; no history/ranking/score; new in 5.4) + Debt & credit (all 8) + Productivity & Competitiveness (6 OECD-covered countries; CHN/IND safe empty state) + Big Cycle Forces entry card ("View force coverage →")
- /country/[iso3]/forces — all 17 Big Cycle forces with data-coverage statuses (Data available / Partially measurable / Indicators defined · data source pending / Data not yet available), live inputs with source labels, catalog-only candidates, dynamic coverage summary; no gauges, no score colors, no Rise/Peak/Decline

## API endpoints

- GET /health
- GET /api/countries (incl. observation_count, indicator_count, latest_observation_year)
- GET /api/countries/{iso3}
- GET /api/countries/{iso3}/observations (latest vintage only, ?indicator=/?source=/?start=/?end= filters)
- GET /api/countries/{iso3}/force-coverage (17 forces, statuses + live/candidate inputs — never scores/phases)
- GET /api/indicators
- GET /api/data/sources
- GET /api/data/ingestion-runs

## Data sources connected

- World Bank — end-to-end, status `testing` (adapter, 15 mappings — 3 GDP + 5 trade/investment (5.1) + 3 WGI governance scores (5.2) + GINI_INDEX + INFLATION_CPI (5.3) + 2 SIPRI-derived military series (5.4), persistence with revision handling, IngestionRun, all 8 countries imported, coverage UI); CLI: `uv run --no-sync python scripts/ingest_world_bank.py --all-countries --all-mapped --start 2000 --end 2025` (from apps/api) or `--indicator CODE` per indicator (WGI: `--indicator RULE_OF_LAW_WGI_SCORE --start 1996 --end 2025`, repeat per indicator; Gini: `--indicator GINI_INDEX --start 1990 --end 2025`; CPI: `--indicator INFLATION_CPI --start 1990 --end 2025`; military: `--indicator MILITARY_EXPENDITURE_USD --start 1990 --end 2025`, repeat with MILITARY_EXPENDITURE_GDP). SEPARATE auxiliary CLI (Sprint 5.15): `uv run --no-sync python scripts/ingest_wgi_diagnostics.py --all-countries --all-indicators --all-kinds --start 1996 --end 2025` — imports the WGI LB/UB/SR diagnostics into `indicator_diagnostics` only (never the canonical observation path; these series are NOT in `--all-mapped`). Status stays `testing` until Milestone 4 hardening (scheduling, monitoring).
- BIS — end-to-end, status `testing` (adapter + 2 approved+seeded mappings + persistence + IngestionRun + frontend Debt & credit section). Official BIS Stats API v1 (`https://stats.bis.org/api/v1`, no auth, SDMX-CSV via `?format=csv`); series keys use ISO2 codes; external identity = `{dataflow}/{key_template}` (e.g. `WS_CREDIT_GAP/Q.{cc}.P.A.C`) with the `{cc}` placeholder kept in SourceSeries (country lives on Observation); quarterly observations use quarter-start `observation_date`; every CSV row is dimension-validated. Import: `$env:PYTHONPATH='.'; uv run --no-sync python scripts/ingest_bis.py --all-countries --all-mapped --start 2000 --end 2025` (from apps/api). Smoke: `PYTHONPATH=. uv run --no-sync python scripts/bis_smoke.py CHE`. Details: DATA_SOURCES.md.
- OECD — end-to-end, status `testing`. Official OECD SDMX REST API (`https://sdmx.oecd.org/public/rest`, no auth, documented ~60 queries/hour — the batch CLI throttles with a 1s inter-request delay and never retries 4xx): `GET /data/{agency},{dataflow},{version}/{key}?format=csvfilewithlabels`. Key findings: v2 `c[...]` filters silently broken (wrong-country rows) → v1 exact dot-keys only; `DF_PDB_LV` broken server-side → `DF_PDB`; REF_AREA is FIRST key dim, ISO3 1:1 (no BIS-style ISO2 translation); a no-observation key answers HTTP 404 body `NoRecordsFound` → `DataSourceNoDataError` → zero-data SUCCESS IngestionRun (`run_metadata.no_data=true`). 2 owner-approved indicators (2026-09-08, seeded): LABOUR_PRODUCTIVITY_PER_HOUR (`DSD_PDB@DF_PDB` v2.0, `{cc}.A.GDPHRS._T.USD_PPP_H.LR.N._Z.PPP`, USD PPP/hour, annual; PRICE_BASE=LR label still requires OECD metadata confirmation — raw code preserved) and UNIT_LABOUR_COST_GROWTH (`DSD_PDB@DF_PDB_ULC_Q` v1.0, `{cc}.Q.ULCE._T.PA.V.GY.S.NC`, % per annum, quarterly; growth chosen over the base-2015 index — index levels never comparable across countries). Imported 1990–2025 for USA/CHE/DEU/FRA/GBR/JPN (1030 obs); CHN and IND have no observations in these selected OECD dataflows (legitimate no-data, never failed runs). Import: `$env:PYTHONPATH='.'; uv run --no-sync python scripts/ingest_oecd.py --all-countries --all-mapped --start 1990 --end 2025` (from apps/api). Smoke: `$env:PYTHONPATH='.'; uv run --no-sync python scripts/oecd_smoke.py CHE`. Details: DATA_SOURCES.md.

## Data sources pending

- FRED/ALFRED, Eurostat, ECB, SNB, UN Comtrade (Milestone 4)

## Database status

PostgreSQL 18 on host, db `bigcycleatlas_dev`, app user `bigcycleatlas_app` (credentials in apps/api/.env). Alembic at head (`e3a7c94b1d51` — the indicator_diagnostics migration added in Sprint 5.14; no new migration in 5.15 — the diagnostic import is data, not schema). Seeded: 8 countries, 8 data sources, 25 indicators, 19 source series (15 WB + 2 BIS + 2 OECD). Live data: 5647 observations, all vintage 1; 0 duplicate identity groups — WB 2953 (GDP trio 264 + trade/investment 1031 + WGI governance 624 + Gini 187 + CPI 287 + military 560) + BIS 1664 + OECD 1030; PLUS 1872 auxiliary `indicator_diagnostics` rows (Sprint 5.15 WGI LB/UB/SR: 8 countries × 3 indicators × 3 kinds × 26 score years 1996–2024, all vintage 1, 0 duplicate identities — no SourceSeries/Observation rows). IngestionRuns: 311 success / 16 failed (failures are all historical: transient WB HTTP errors, the two racing batches, and 5 OECD HTTP-429 responses; the 75 diagnostic runs added in 5.15 — 72 first import + 3 idempotent re-runs — are all success).

### Data incident (2026-09-08, resolved)

Two concurrent batch imports raced (an orphaned pre-compaction background run plus the sprint batch): both checked "no existing observation" and inserted the same vintage-1 rows. 110 exact-duplicate observations resulted (values verified identical). Cleanup (owner-approved): duplicates deleted keeping the earliest row; `uq_observations_identity` unique constraint added so the failure mode is now physically impossible. Lesson: never run two batch imports at once — the unique constraint now guards this anyway.

## Cycle model status

Coverage foundation built (Milestone 5.0) + trade/investment promotion (Milestone 5.1) + WGI governance promotion (Milestone 5.2) + inequality/CPI promotion + coverage ceiling (Milestone 5.3) + military proxy promotion (Milestone 5.4, 2026-09-08); scoring NOT built. Phase/stage enum and 17-force definitions documented in docs/data-model.md, shared constants in packages/shared, and formalized as a typed config layer in `app/cycle/force_definitions.py` (wording preserved). Live indicator→force mapping after 5.4: Productivity / output growth (GDP_GROWTH + LABOUR_PRODUCTIVITY_PER_HOUR), Cost competitiveness (UNIT_LABOUR_COST_GROWTH + INFLATION_CPI), Rule of law (RULE_OF_LAW_WGI_SCORE), Corruption (CONTROL_OF_CORRUPTION_WGI_SCORE — higher = less corruption, never reversed), Trade and capital flows (EXPORTS_GDP + IMPORTS_GDP + TRADE_BALANCE + CURRENT_ACCOUNT_GDP), Infrastructure and investment (GROSS_CAPITAL_FORMATION_GDP), Indebtedness (CREDIT_TO_GDP_GAP + DEBT_SERVICE_RATIO), Military strength (MILITARY_EXPENDITURE_USD + MILITARY_EXPENDITURE_GDP, capped at PARTIAL by DEC-009/DEC-011 — spending is an input proxy, never capability), Wealth / opportunity / values gaps (GINI_INDEX, capped at PARTIAL by DEC-009), Internal conflict (POLITICAL_STABILITY_WGI_SCORE, capped at PARTIAL by DEC-009). Three forces carry a conceptual coverage ceiling (proxy-only mappings can never report AVAILABLE — a deliberate methodology guard); lifting a ceiling is a future owner decision (WID wealth shares / ACLED event data / military capability data — more spending series will NOT lift the Military-strength ceiling). Candidates remain for education (Option C decided, better indicators pending), government debt (general-government source pending), global openness (deliberately not promoted), and strengthening inputs. WGI, Gini, and military-spending values are SOURCE indicators — never Atlas force scores. No weights, no force scores, no phases. (25 canonical data indicators ≠ 17 forces; 7 of 17 forces are fully measurable for OECD-6 countries + 3 partial by ceiling; CHN/IND 5 available + 5 partial. **Normalization state: Sprint 5.5 design (`.dev/NORMALIZATION.md` + typed registry) + Sprint 5.6 FIRST EXECUTABLE PATH — as-of alignment + freshness + DIRECT_0_100 level signals for the WGI ×3 (`app/cycle/normalizer.py`) — + Part 0 PERIOD-COMPLETE ALIGNMENT (DEC-015): eligibility = `effective_period_end <= scoring_period_end` (annual first eligible at its own Q4; raw rows never modified) + Sprint 5.7 MOMENTUM (DEC-016): OWN_HISTORY signed raw-point change for the WGI ×3, windows (3, 5) with 5y primary, anchor tolerance 1 annual period, anchors via the same align_observation_as_of path, MomentumWindowResult provenance, missing history → None never 0; model version `normalization-v0.3` (method `sprint-5.7-wgi-momentum-r1`) + Sprint 5.8 RELATIVE (DEC-017): CROSS_SECTIONAL_RELATIVE mid-rank positions within the frozen typed tracked_8 universe for the WGI x3 (100 * (average_rank - 0.5) / n, ties = average rank, complete-universe rule 8/8-or-nobody, usable/expected provenance, freshness gates usability but never scales, NOT a global percentile); model version `normalization-v0.4` (method `sprint-5.8-wgi-tracked8-relative-r1`); computed on demand, never persisted, backtest_safe=False; all non-WGI indicators raise NormalizationNotImplementedError; confidence remains None. WGI momentum is NOT approved for direct cross-indicator aggregation.** + **Sprint 5.9 (DEC-018)**: relative helper hardened with its own execution gate (level DIRECT_0_100 AND relative CROSS_SECTIONAL_RELATIVE → exactly the WGI ×3 — a direct `build_relative_cross_section` call for any non-approved indicator raises NormalizationNotImplementedError); non-WGI level audit recorded in NORMALIZATION.md — GDP_GROWTH / GCF / INFLATION_CPI / UNIT_LABOUR_COST_GROWTH RECLASSIFIED to CONTEXTUAL_DEFERRED (level), Gini direction confirmed without a numeric curve, credit-gap asymmetric TARGET_BAND confirmed with thresholds unresolved (BIS research question recorded), DSR OWN_HISTORY confirmed and SELECTED AS THE NEXT IMPLEMENTATION TARGET, productivity direction confirmed with the absolute level curve deferred pending expanded calibration; research support `scripts/normalization_profile.py` (read-only descriptive stats); no model-version bump (WGI outputs unchanged).** + **Sprint 5.10 (DEC-019)**: DSR OWN_HISTORY level IMPLEMENTED for exactly DEBT_SERVICE_RATIO — expanding as-of own-history calibration of real latest-vintage observations (inclusive of current, DEC-015 eligibility, no future data, no forward-fill), empirical mid-rank stress percentile (ties = average rank), level_score = 100 − stress_percentile, minimum 20 observations (Atlas MODEL PARAMETER — below → None never 0), endpoints not forced, freshness gates current only and never scales, OwnHistoryLevelResult provenance, execution gated by the versioned own_history_level_configs entry (registry alone never auto-enables); model version `normalization-v0.5` (method `sprint-5.10-dsr-own-history-level-r1`); WGI level/momentum/relative unchanged; DSR relative prohibited (BIS caution), DSR momentum not implemented, confidence None; on demand only, never persisted, backtest_safe=False.** + **Sprint 5.11 (DEC-020)**: credit-gap evidence audit — CREDIT_TO_GDP_GAP level family RECLASSIFIED asymmetric TARGET_BAND → ONE_SIDED_VULNERABILITY (positive-side breakpoints supported by Basel CCyB guide +2/+10 and BIS 2018 EWI ~9/4–9 amber; negative-side penalty NOT supported and retracted — the guide is flat zero below +2 and a persistent negative gap is a boom-contaminated-trend artifact); the credit gap STILL raises NormalizationNotImplementedError; NO numeric Atlas breakpoints approved; no model-version bump (v0.5).** + **Sprint 5.12 (DEC-021)**: credit-gap ONE_SIDED_VULNERABILITY level IMPLEMENTED for exactly CREDIT_TO_GDP_GAP — the owner-approved curve (flat 50 at/below +2pp, strictly linear to 0 at +10pp, clamped above; no-excess region deliberately NEUTRAL 50, never 100; negative gaps carry no penalty), versioned as OneSidedVulnerabilityConfig model parameters (normalization-v0.6, method `sprint-5.12-credit-gap-one-sided-level-r1`) with OneSidedVulnerabilityResult provenance, execution gated by the explicit one_sided_vulnerability_configs entry (registry family alone never auto-enables), no minimum-history gate (parametric curve), freshness gates current only and never scales, Basel-caution score-interpretation limits carried; WGI level/momentum/relative unchanged, DSR unchanged; credit-gap relative stays CONTEXTUAL_DEFERRED (None) and momentum windows (4q/8q) stay UNAPPROVED (momentum None); confidence None; on demand only, never persisted, backtest_safe=False.**)

## Known issues

- design.md is gitignored by owner request (kept local only).
- Do not run `npm run build` while `next dev` is serving — the production build overwrites `.next` and the dev server serves 404 chunks until restarted. **Happened 2026-09-08** (post-5.0 build against a serving dev server): localhost:3000 404s every `/_next/static` chunk and pages render unstyled/stuck. Fix: stop `next dev`, restart it (`npm run dev -w apps/web`) — dev rebuilds `.next` on boot. Run future builds only with dev stopped.
- **RESOLVED (2026-09-08, M5.3): the stale :8000 backend.** The M5.2 diagnosis (a pre-M5.0 process serving old routes) was correct but incomplete: the owner's `Stop-Process` killed the uvicorn *reloader parent*, while its multiprocessing spawn **worker survived as an orphan, still holding the shared listening socket and serving old code** — which is why the live route kept 404ing after the "restart". **Windows uvicorn hazard: `Stop-Process` on a reload-mode uvicorn parent does NOT kill the spawn worker.** To stop the backend reliably, kill the *process tree*: identify the worker via Win32_Process (`Get-CimInstance Win32_Process | Where-Object CommandLine -match uvicorn`), kill parent and children, and verify the port is actually free (`Get-NetTCPConnection -LocalPort 8000 -State Listen`) before restarting. Same applies to `next dev` on :3000.
- OECD documents ~60 queries/hour and enforces it (HTTP 429 observed mid-batch); run OECD batch imports sequentially with the built-in delay and re-import only missing series after a cooldown — never retry-storm.

## Test status

`pytest` in apps/api — 371 passed, 0 failed (2026-09-09, Sprint 5.15: 340 baseline (Sprint 5.14) + 31 new offline tests in tests/test_wgi_diagnostic_ingestion.py — the 9-spec registry check, fetch URL/params/provider-source identity, metadata sourceid + record indicator/country identity rejection, null/non-finite handling, raw_payload preservation, DTO-type isolation, structural source-separation asserts over the three modules, persistence vintages (insert/skip/immutable revision), non-integer SR rejection through the full path, transient-only retry semantics (5xx/statusless retryed; 4xx/parse/identity never; gives up after 3), IngestionRun diagnostic metadata, failed-run recording, per-series failure isolation, exact-period lookup + no-fallback, country isolation, catalog/coverage/normalization isolation (Observation/SourceSeries/Indicator/force/WGI-DSR-credit-gap outputs unchanged, confidence None), model version v0.6). All offline; baseline before 5.15 (340, Sprint 5.14). `apps/api/scripts/ingest_wgi_diagnostics.py` is the live import CLI (live WB HTTP + dev DB writes) — manual, not part of pytest. `apps/api/smoke_routes.py` and `apps/api/scripts/normalize_smoke.py` are manual smoke scripts requiring a live DB — not part of pytest (Sprint 5.12 smoke run live + read-only 2026-09-09 on Postgres: --all-tracked CREDIT_TO_GDP_GAP @2025-Q4 — USA −11.54 → 50, CHN −7.69 → 50, CHE −17.04 → 50, DEU −3.96 → 50, FRA −15.11 → 50, GBR −17.82 → 50, JPN +6.78 → 20.10, IND +1.74 → 50, all freshness 1.0, model normalization-v0.6; DSR regression CHE @2025-Q4 → 13.4615 unchanged). `apps/api/scripts/normalization_profile.py` is a read-only research script (live Postgres, SELECT only) — NOT part of pytest, NOT an API endpoint, NOT proof of methodology. `npm run build` in apps/web — SUCCESS 2026-09-08 after M5.4 (no frontend files touched in 5.5–5.12, so not re-run; last Playwright verification 16/16, M5.4).

### Bug fix (2026-09-08, post-sprint verification)

`/api/countries/{iso3}/observations` leaked cross-country rows once more than one country had data: `list_current_observations` joined the latest-vintage subquery to the outer query only on (source_series_id, period_start, vintage), and SourceSeries rows are shared across countries — the outer and count queries lacked a country_id filter. Fixed by adding the country filter to both outer scopes; 3 regression tests added. Verified live: USA/CHE/CHN/IND endpoints and pages each return only their own values (see ISSUES.md ISSUE-004).

## Files changed in latest session (Sprint 5.17, Gini calibration universe + comparability audit)

- apps/api/scripts/gini_calibration_profile.py (NEW — read-only profile: tracked_8 Gini coverage + welfare-concept hint + read-only live WB API v2 query of the global Gini universe; 187 tracked_8 obs + 2430 global obs/171 countries; no writes, no new connector, no scores)
- .dev/NORMALIZATION.md (+ Sprint 5.17 section with provider semantics, tracked_8 audit, calibration universe options matrix, global data audit, distribution stability, survey-concept comparability, transformation families, midpoint semantics, decision matrix, verdict DEFER_GINI_LEVEL; §7 GINI_INDEX row updated with DEC-024 deferral)
- .dev/DECISIONS.md (+ DEC-024: Gini numeric level DEFERRED — durable methodology decision with 6 findings + required evidence/data/methodology list)
- .dev/DATA_SOURCES.md (Gini welfare-concept clarification: income-based vs consumption-based; WB API does NOT expose per-observation welfare-concept tag)
- .dev/HANDOFF.md (this file — Sprint 5.17 lead paragraph; Sprint 5.16 lead paragraph; In progress / Next task; files-changed section)
- .dev status files (PROJECT_STATUS, DEVLOG, BACKLOG)

No frontend, API-route, model, migration, mapping, seed, or canonical raw-data changes. No Gini level_score implemented; model version stays normalization-v0.6; confidence stays None everywhere; Wealth-gap force stays PARTIAL (DEC-009). pytest 373 passed (unchanged — docs + read-only research only).

## Files changed in earlier session (Sprint 5.16, WGI confidence calibration audit + diagnostic hardening)

- apps/api/app/services/wgi_diagnostic_ingestion.py (connection_invalidated propagation: recoverable DBAPIError -> FAILED run; DBAPIError.connection_invalidated=True -> raise, no false audit promise; docstring updated)
- apps/api/tests/test_wgi_diagnostic_ingestion.py (+1 new regression: test_connection_invalidated_dbapi_error_propagates; 33 total)
- apps/api/scripts/wgi_confidence_profile.py (NEW — read-only empirical profile: dataset integrity, CI width, boundary clipping, score-vs-width, source count, width/SR overlap, dimension/country/time differences, calibration option matrix, freshness composition, source quality, method sufficiency; 624 rows analyzed; no writes, no scores)
- .dev/NORMALIZATION.md (+ Sprint 5.16 section with empirical profile, calibration option matrix, verdict DEFER_NUMERIC_CONFIDENCE; §17 item 6 updated with deferral)
- .dev/DECISIONS.md (+ DEC-023: WGI numeric indicator confidence DEFERRED — durable methodology decision with 6 empirical findings + required evidence/data/methodology list)
- .dev/HANDOFF.md (this file — Sprint 5.16 lead paragraph; Sprint 5.15.1 lead paragraph; In progress / Next task; files-changed section)
- .dev status files (PROJECT_STATUS, DEVLOG, BACKLOG)

No frontend, API-route, model, migration, mapping, seed, or canonical raw-data changes. No numeric confidence implemented; model version stays normalization-v0.6; confidence stays None everywhere. pytest 373 passed (372 + 1 new connection_invalidated regression).

## Files changed in earlier session (Sprint 5.15, WGI diagnostic live ingestion)

- apps/api/app/data_sources/world_bank_wgi_diagnostics.py (NEW — dedicated fetch path: WorldBankWgiDiagnosticFetcher with the adapter's SSL/timeout conventions, `?source=3` on the plain v2 endpoint, metadata sourceid + record indicator.id/countryiso3code identity validation, null/non-finite handling, YYYY-01-01 periods, raw_payload preserved; produces ONLY IndicatorDiagnosticDTO)
- apps/api/app/services/wgi_diagnostic_ingestion.py (NEW — run_wgi_diagnostic_ingestion: DataSource resolution by source_key, IngestionRun with data_kind="indicator_diagnostic" + base indicator/kind/provider series/provider source/year span in run_metadata, persist_indicator_diagnostics ONLY inside a SAVEPOINT, fetch + expected persistence/DB failures recorded as failed runs via non-raising outcomes; Sprint 5.15.1 hardened the persistence-failure audit path)
- apps/api/scripts/ingest_wgi_diagnostics.py (NEW — separate batch CLI: --country/--all-countries, --indicator/--all-indicators, --kind/--all-kinds, --start/--end; TransientRetryFetcher (network/5xx only, max 3 attempts); per-country×series transactions; failure isolation; batch summary + exit 1 on any failure; Windows event-loop policy)
- apps/api/tests/test_wgi_diagnostic_ingestion.py (NEW — 33 offline tests covering the brief's Part-21 items + the Sprint 5.15.1 audit-hardening regressions: fetch/identity/DTO semantics, persistence vintages, retry semantics, IngestionRun behavior, isolation (country, catalog, coverage, normalization), exact-period lookup, idempotency, immutable revision, structural separation source-text asserts, non-integer source-count FAILED-run audit, DB-failure savepoint rollback + clean-session recovery, connection_invalidated DBAPIError propagation; httpx MockTransport, no external HTTP)
- .dev/DATA_SOURCES.md (WGI methodology line: LIVE INGESTION COMPLETE 2026-09-09 — row counts, year coverage, idempotency, provider source identity, no-canonical-pollution; + diagnostic-CLI line; Last tested → 2026-09-09)
- .dev/NORMALIZATION.md (+ "Sprint 5.15 implementation status" section (4 implemented items + NOT-done paragraph incl. the CHE RL lookup smoke with CI width display-only); §17 item 9 → RESOLVED (live ingestion complete; item 6 confidence composition remains open); §10.2 measurement_uncertainty row updated)
- .dev/HANDOFF.md (this file — Sprint 5.15.1 lead paragraph; Sprint 5.15 lead paragraph; Completed entry; In progress / Next task; Database status (head e3a7c94b1d51, 1872 diagnostic rows, 311/16 runs); World Bank source entry (+diagnostics CLI); Test status 372; files-changed section)
- .dev status files (PROJECT_STATUS, DEVLOG, BACKLOG)

No frontend, API-route, model, migration, mapping, seed, or canonical raw-data changes (the 1872 diagnostic rows are the deliberate Sprint 5.15 import — the canonical 25/19/5647 verified unchanged). No DEC-023 (ordinary ingestion mechanics under DEC-022). No npm build run (no frontend files touched). Live verification: 72/72 series SUCCESS (1872 received/inserted, 0 skipped/revised, all vintage 1, 0 duplicate identities, 26 score years 1996–2024 with the 1997/1999/2001 gaps); LB ≤ score ≤ UB 624/624 (0 violations); SR all integer-valued, observed 4–16; one-to-one score-year alignment 0 problems; idempotent re-run CHE RL 0 inserted / 78 skipped / 0 revised; exact-period lookup smoke CHE RL @2025-Q2 → 2024 score 87.3184, LB 82.0371, UB 92.5997, CI width 10.5626 (display only), SR 10; 2025 lookup → all None. pytest 371 passed, 0 failed.

## Files changed in earlier session (Sprint 5.12, credit-gap ONE_SIDED_VULNERABILITY level)

- apps/api/app/cycle/normalization_definitions.py (+`OneSidedVulnerabilityConfig` frozen dataclass (neutral_ceiling, saturation_value, no_excess_score, saturated_score; post-init: saturation > ceiling, scores in 0–100, stress must not fall above the ceiling) + `OneSidedVulnerabilityResult` provenance (curve params + level_score, config re-validated); `NormalizedSignal.one_sided_vulnerability_level` field + docstring updated; `ModelVersionConfig.one_sided_vulnerability_configs` with the execution-gate docstring; CURRENT_MODEL_VERSION bumped normalization-v0.5 → **v0.6**, method `sprint-5.12-credit-gap-one-sided-level-r1`, version-history comment, one_sided_vulnerability_configs={"CREDIT_TO_GDP_GAP": (2.0, 10.0, 50.0, 0.0)} — all WGI + DSR config unchanged; `validate_normalization_registry()` gained the one-sided config↔registry gate; CREDIT_TO_GDP_GAP registry notes updated to IMPLEMENTED with the curve)
- apps/api/app/cycle/normalizer.py (+`one_sided_vulnerability_level(value, config)` pure curve function (flat no_excess at/below ceiling, linear between, clamped at saturation); dispatcher routes one_sided_vulnerability → `_normalize_one_sided_vulnerability_as_of` (config gate raises "not auto-enabled" → align_observation_as_of → None if no eligible observation → freshness gate → curve → NormalizedSignal with provenance, relative None (CONTEXTUAL_DEFERRED), momentum None (4q/8q unapproved), confidence None); not-implemented error message lists the new executable set; module docstring documents Sprint 5.12)
- apps/api/scripts/normalize_smoke.py (`_print_signal` one_sided_vulnerability branch — score to 2 decimals + the curve-parameter line with the neutral/saturation interpretation; CREDIT_TO_GDP_GAP Basel-caution note — common reference point, NOT a mechanical rule, relative deferred, momentum unapproved; header sprint range 5.6–5.12)
- apps/api/tests/test_normalization_signals.py (Sprint 5.12 suite REPLACES the 5.11 still-unscored regression: OWNER_CURVE_TABLE exact regression, pure-curve clamps/continuity/config-validation, no-auto-enable gate, no-observation → None, too-stale → None, freshness-decays-but-never-scales, other-dimensions-stay-None + provenance; imports +OneSidedVulnerabilityConfig/one_sided_vulnerability_level; model-version test → v0.6 + exactly-CREDIT_TO_GDP_GAP config assertions)
- apps/api/tests/test_dsr_own_history.py (version literals → v0.6; normalization_method assertion updated; DSR config asserted unchanged by 5.12)
- apps/api/tests/test_relative_scores.py (version literals → v0.6)
- .dev/DECISIONS.md (+DEC-021 — the owner-approved curve with the verbatim table, the neutral-50 no-excess interpretation, the threshold-concept separation, and the impact incl. live smoke values; recommended Sprint 5.13 candidates)
- .dev/NORMALIZATION.md (+ "Sprint 5.12 implementation status" section: the curve table verbatim, the locked design decisions (neutral ceiling, 50-not-100, no negative penalty, endpoint clamps, no minimum-history gate, freshness semantics, unchanged dimensions), Basel-caution score-interpretation limits, live smoke values, test inventory; §6 ONE_SIDED_VULNERABILITY row marked IMPLEMENTED; §7 CREDIT_TO_GDP_GAP row rewritten; §17 item 2 RESOLVED per DEC-021; Sprint-5.6 "current truth" closer + Sprint-5.5 closing-scope note updated to v0.6 + credit-gap implemented, 14 remaining non-WGI indicators raise)
- .dev/HANDOFF.md (this file — Sprint 5.12 lead paragraph; Completed entry; In progress / Next task; Cycle model status; Test status 318; files-changed section)
- .dev status files (PROJECT_STATUS, DEVLOG, BACKLOG)

No frontend, API-route, model, migration, mapping, seed, or raw-data changes. No npm build run (no frontend files touched). Live verification read-only (nothing written; all values computed from the live DB; no hardcoded expected scores): --all-tracked CREDIT_TO_GDP_GAP @2025-Q4 — USA −11.54 → 50, CHN −7.69 → 50, CHE −17.04 → 50, DEU −3.96 → 50, FRA −15.11 → 50, GBR −17.82 → 50, JPN +6.78 → 20.10, IND +1.74 → 50 (all freshness 1.0, backtest safe false); DSR regression CHE @2025-Q4 → 13.4615 (rank 90.5/104) unchanged. All output model version normalization-v0.6. pytest 318 passed.

## Files changed in earlier session (Sprint 5.11, credit-to-GDP gap evidence audit)

- apps/api/app/cycle/normalization_definitions.py (METHODOLOGY ONLY — new `NormalizationFamily.one_sided_vulnerability` enum member with a documented rationale (one-sided vulnerability mapping: no stress at or below a neutral ceiling, stress rises monotonically above it); CREDIT_TO_GDP_GAP level family reclassified target_band → one_sided_vulnerability with the full evidence-audit note (positive-side breakpoints supported by Basel +2/+10 and BIS 2018 EWI ~9/amber 4–9; negative-side penalty NOT supported and RETRACTED per DEC-020; numeric Atlas breakpoints unresolved pending owner approval — NOT implemented). Relative/momentum/freshness unchanged; CURRENT_MODEL_VERSION unchanged (normalization-v0.5 — no score semantics became executable); registry validation unchanged.)
- apps/api/tests/test_normalization_signals.py (Sprint-5.9 exact-classification table updated: CREDIT_TO_GDP_GAP → one_sided_vulnerability with a DEC-020 comment; +1 new regression `test_credit_to_gdp_gap_still_unscored_after_sprint_5_11` — the reclassified family still raises NormalizationNotImplementedError and no numeric breakpoint exists)
- .dev/DECISIONS.md (+DEC-020 — the reclassification with the full positive-side/negative-side evidence basis; NO provisional numeric Atlas breakpoints recorded)
- .dev/NORMALIZATION.md (Part 0: the Sprint-5.6 closer now states current truth v0.5 + DSR implemented + "every OTHER non-WGI level indicator" wording; the Sprint-5.5 closing-scope note updated likewise; + "Sprint 5.11 — Credit-to-GDP gap evidence audit" section: verified provider definition, three separated threshold concepts, positive-side evidence, negative-side evidence (no threshold; artifact documented), Basel caution, as-of semantics (one-sided filter ≠ release-date safety), construction-vs-normalization history, descriptive tracked_8 profile + bucket counts, the full evidence matrix, and the RECLASSIFY verdict with the owner-approval list; §6 family table gained ONE_SIDED_VULNERABILITY and the TARGET_BAND row updated; §7 CREDIT_TO_GDP_GAP row rewritten; §17 item 2 updated to the Sprint 5.11 outcome)
- .dev/HANDOFF.md (this file — Sprint 5.11 lead paragraph; Completed entry; In progress / Next task; Cycle model status; Test status 312; files-changed section)
- .dev status files (PROJECT_STATUS, DEVLOG, BACKLOG)

No frontend, API-route, model, migration, mapping, seed, or raw-data changes; no executable scoring change (the credit gap raised NormalizationNotImplementedError before and after). No npm build run (no frontend files touched). Live verification read-only (nothing written): normalization_profile.py --indicator CREDIT_TO_GDP_GAP + an inline read-only bucket-count query (832 observations — descriptive only). pytest 312 passed.

## Files changed in earlier session (Sprint 5.10, DSR OWN_HISTORY level)

- apps/api/app/cycle/normalization_definitions.py (+OwnHistoryLevelResult / OwnHistoryLevelConfig frozen dataclasses with post-init validation; NormalizedSignal.own_history_level provenance field; ModelVersionConfig.own_history_level_configs with the execution-gate docstring; CURRENT_MODEL_VERSION bumped normalization-v0.4 → **v0.5**, method `sprint-5.10-dsr-own-history-level-r1`, version-history comment documenting the DSR choices, own_history_level_configs={"DEBT_SERVICE_RATIO": min 20} — all WGI v0.3/v0.4 config unchanged; validate_normalization_registry() cross-checks own_history_level_configs against the registry (lives there because NORMALIZATION_REGISTRY is defined after CURRENT_MODEL_VERSION))
- apps/api/app/services/alignment_service.py (+`own_history_as_of(session, country_iso3, indicator_code, scoring_period)` — as-of own-history selection with IDENTICAL semantics to align_observation_as_of (country + indicator lookup, latest-vintage subquery, DEC-015 period-complete, period_start <= as_of), returning ALL eligible rows ordered period_start asc (vintage desc, id desc); the last element IS the current aligned observation; read-only, no forward-fill, no synthetic rows)
- apps/api/app/cycle/normalizer.py (dispatcher refactor: normalize_indicator_as_of routes direct_0_100 → `_normalize_direct_0_100_as_of` (the WGI body, byte-equivalent behavior — UNCHANGED) and own_history → `_normalize_own_history_as_of` (config gate → own_history_as_of → freshness gate → min-20 gate → score); all other level families raise NormalizationNotImplementedError listing the executable set; +`own_history_stress_position` pure mid-rank function (membership-checked, tie-averaging, order-independent, empty-sample guarded); module docstring updated for 5.10)
- apps/api/scripts/normalize_smoke.py (--all-tracked mode looping TRACKED_8_MEMBERS with === ISO3 === headers and per-country failure counting; `_print_signal` gained the own_history block (level or not-available-with-counts, sample n + minimum, calibration span, rank + stress percentile + formula note) and "Momentum: not calculated" when no windows; per-method freshness note (historical anchors and calibration points are never decayed); DSR note (cross-country raw-DSR ranking prohibited); ASCII-only)
- apps/api/tests/test_dsr_own_history.py (NEW — 26 offline tests; synthetic BIS-style quarterly DSR DTOs via the existing persist_observations helper; covers all brief Part-19 areas)
- apps/api/tests/test_normalization_signals.py (v0.4 → v0.5 model-version test: version_id, method, own_history_level_configs == {DEBT_SERVICE_RATIO: min 20})
- apps/api/tests/test_relative_scores.py (model-version assertion v0.4 → v0.5)
- .dev/DECISIONS.md (+DEC-019 — the full locked methodology incl. the explicit statement that minimum 20 is an ATLAS VERSIONED MODEL PARAMETER, NOT BIS methodology truth)
- .dev/NORMALIZATION.md (§7 table rewritten to agree with DEC-018 (Part 0 of the brief); + "Sprint 5.10 implementation status" section: DSR OWN_HISTORY LEVEL = IMPLEMENTED, DSR relative = None (prohibited), DSR momentum = not implemented, confidence = None, NO Indebtedness force scoring exists)
- .dev/HANDOFF.md (this file — Sprint 5.10 lead paragraph; Completed entry; In progress / Next task; Cycle model status; Test status 311; files-changed section)
- .dev status files (PROJECT_STATUS, DEVLOG, BACKLOG)

No frontend, API-route, model, migration, mapping, seed, or raw-data changes. No npm build run (no frontend files touched). Live verification read-only (nothing written; all values computed from the live DB; no hardcoded expected scores): --all-tracked DSR @2025-Q4 — USA 14.1 → 94.7115 (rank 6/104), CHN 18.8 → 2.88462 (rank 101.5/104), CHE 19.7 → 13.4615, DEU 12.2 → 88.9423, FRA 20.8 → 4.32692 (rank 100/104), GBR 13.1 → 96.1538, JPN 15.5 → 25, IND 12.2 → 43.75 — all n=104, calibration 2000-Q1..2025-Q4, freshness 1.0000; CHE @2004-Q3 → raw 15.6, n=19, level None (not zero); CHE @2004-Q4 → n=20, first score 97.5. All output model version normalization-v0.5, backtest safe: false. pytest 311 passed.

## Files changed in earlier session (Sprint 5.9, normalization audit + relative hardening)

- apps/api/app/cycle/relative.py (HARDENING — `build_relative_cross_section` now enforces its own execution gate BEFORE any alignment/ranking: level family DIRECT_0_100 AND relative family CROSS_SECTIONAL_RELATIVE, which resolves to exactly the WGI ×3 under the current registry — a direct helper call for any non-approved indicator (e.g. GINI_INDEX) raises NormalizationNotImplementedError instead of silently ranking it. No WGI codes hardcoded; complete-universe / freshness / 0..100-validation rules untouched; WGI numerical outputs unchanged; read-only as before.)
- apps/api/app/cycle/normalization_definitions.py (REGISTRY AUDIT per DEC-018: GDP_GROWTH, GROSS_CAPITAL_FORMATION_GDP, INFLATION_CPI, UNIT_LABOUR_COST_GROWTH level families → CONTEXTUAL_DEFERRED with audited notes; GINI_INDEX stays MONOTONIC_NEGATIVE with "no numeric curve approved — 100 − Gini NOT an approved mapping" note; CREDIT_TO_GDP_GAP stays asymmetric TARGET_BAND with thresholds-unresolved note; DEBT_SERVICE_RATIO stays OWN_HISTORY + READY_FOR_IMPLEMENTATION_DESIGN note; LABOUR_PRODUCTIVITY_PER_HOUR stays MONOTONIC_POSITIVE with level-curve-deferred note. NO model-version change — CURRENT_MODEL_VERSION remains normalization-v0.4 / sprint-5.8-wgi-tracked8-relative-r1, momentum config untouched.)
- apps/api/app/cycle/freshness.py (docstring only — decay shape documented as SETTLED exponential per DEC-013; linear implementation retained as an available alternative for tests/future versions, NOT current methodology)
- apps/api/scripts/normalization_profile.py (NEW — READ-ONLY research support: per-country + pooled descriptive statistics (count, earliest/latest, min/p10/p25/median/p75/p90/max) for the 8 audited indicators; latest-vintage dedup; NO scores, NO writes, NO API; explicitly NOT proof that an empirical percentile is economic truth)
- apps/api/tests/test_relative_scores.py (+5 hardening regressions: direct helper rejects GINI_INDEX / GDP_GROWTH / GCF / LABOUR_PRODUCTIVITY_PER_HOUR and a contextual_deferred-relative indicator; WGI direct-helper mid-rank output unchanged (93.75 / 6.25); complete-universe rule unchanged via direct helper; no DB writes)
- apps/api/tests/test_normalization_signals.py (+2 registry regressions: exact post-audit level families for all 8 audited indicators; the four reclassified indicators still raise NormalizationNotImplementedError)
- .dev/DECISIONS.md (+DEC-018 — the four reclassifications, Gini direction-without-curve, credit-gap family confirmation with thresholds unresolved, DSR selected as next implementation target, productivity direction confirmed with absolute level deferred; NO numeric thresholds approved)
- .dev/NORMALIZATION.md (+ "Sprint 5.9 — relative hardening + Non-WGI Level Parameter Decision Audit" section with the full audit table; §5 decay shape marked RESOLVED per DEC-013 with linear kept as an alternative; §17 items 2/3/7 updated to the audit outcomes and item 10 (WGI biennial gap) marked RESOLVED FOR THE CURRENT WGI PATH ONLY; stale Sprint-5.5/5.6 "nothing computed" closers corrected; "other 15/16 indicators" → "non-WGI indicators")
- .dev/HANDOFF.md (this file — Sprint 5.9 lead paragraph; DEC-005/DEC-006 decision-summary labels corrected to DECISIONS.md truth; test status 285; files-changed section)
- .dev status files (PROJECT_STATUS, DEVLOG, BACKLOG)

No frontend, API-route, model, migration, mapping, seed, or raw-data changes. No npm build run (no frontend files touched). Live verification read-only (nothing written, all values computed from the live DB): CHE RULE_OF_LAW @2025-Q2 → level 87.3184, relative 93.75 (rank 8/8, universe 8/8), momentum −2.58971 — UNCHANGED after hardening; CHN RULE_OF_LAW @2025-Q2 → relative 6.25 (rank 1/8); scripts/normalization_profile.py run read-only on live Postgres for the audit's descriptive statistics. All output model version normalization-v0.4, backtest safe: false. pytest 285 passed.

## Files changed in earlier session (Sprint 5.7, WGI momentum)

- apps/api/app/services/alignment_service.py (+`shift_scoring_period_years(scoring_period, years)` — quarter preserved (2025-Q2 − 5y → 2020-Q2); negative years rejected; pure scoring-clock arithmetic, no DB access)
- apps/api/app/cycle/normalization_definitions.py (+`MomentumWindowResult` frozen dataclass (window_years, requested_anchor_period, anchor_source_period, change) with range validation on non-None change; NormalizedSignal + `momentum_window_years` / `momentum_windows` provenance fields; ModelVersionConfig + `momentum_primary_window_years` / `momentum_anchor_tolerance_periods` versioned MODEL PARAMETERS with consistency validation (primary ∈ windows); CURRENT_MODEL_VERSION → `normalization-v0.3` / method `sprint-5.7-wgi-momentum-r1` with windows (3, 5), primary 5y, tolerance 1 annual period for exactly the WGI ×3; backtest_safe stays False)
- apps/api/app/cycle/normalizer.py (+`_momentum_window_result` helper — anchor via align_observation_as_of at the shifted period (DEC-015 inherited), tolerance measured as source-years older than the requested anchor year, out-of-range anchor raises, signed change from aligned RAW values; main flow gains the momentum gate (level DIRECT_0_100 AND momentum OWN_HISTORY → exactly the WGI ×3; anything else raises NormalizationNotImplementedError), per-window computation, primary-5y-only headline with NO fallback/averaging; module docstring documents the DEC-016 semantics)
- apps/api/scripts/normalize_smoke.py (+momentum section: primary window, headline change with sign, explicit not-available branch (NOT zero, no fallback), per-window requested/actual anchor + change, freshness note that anchors are never decayed; ASCII-only output)
- apps/api/tests/test_normalization_signals.py (+18 momentum regressions, 32 → 50 tests in this file; former test_momentum_is_not_calculated renamed test_momentum_stays_none_when_history_is_missing)
- .dev/DECISIONS.md (+DEC-016 WGI momentum)
- .dev/NORMALIZATION.md (+ "Sprint 5.7 implementation status" section; §9 momentum bullet marked RESOLVED FOR THE WGI ×3 ONLY; §17 item 5 sign conventions marked resolved for the WGI ×3 only)
- .dev status files (HANDOFF, PROJECT_STATUS, DEVLOG, BACKLOG)

No frontend, API-route, model, migration, mapping, seed, or raw-data changes. No npm build run (no frontend files touched). Live verification read-only (nothing written, values read from the live DB, nothing hardcoded): CHE RULE_OF_LAW @2025-Q2 → level 87.3184 (2024), momentum −2.58971 (5y, requested anchor 2020-Q2 → actual 2019; 3y: requested 2022-Q2 → actual 2021, −0.443138), freshness 0.9170 stale; CHE RULE_OF_LAW @2001-Q2 → level 91.4538 (2000), momentum None (5y anchor 1996-Q2 → no alignable observation; 3y requested 1998-Q2 → actual 1996, 2 years older → rejected by tolerance); CHN CONTROL_OF_CORRUPTION @2025-Q2 → +2.79934; DEU POLITICAL_STABILITY @2025-Q2 → −10.2986; USA CONTROL_OF_CORRUPTION @2025-Q2 → −1.72217. All output model version normalization-v0.3, backtest safe: false.

## Files changed in earlier session (Part 0, period-complete alignment hardening)

- apps/api/app/services/alignment_service.py (eligibility = `effective_period_end <= scoring_period_end`: new `effective_period_end(period_start, freshness_class)` helper — annual/irregular → YYYY-12-31, quarterly → quarter end; new `_period_complete_condition` index-based SQL expression applied to BOTH the latest-vintage subquery and the outer query, rendering identically on SQLite and PostgreSQL; `period_start <= as_of` kept as a cheap necessary precondition; AlignedValue now carries `effective_period_end`; module + function docstrings document the rule and its no-release-date-safety scope; read-only — raw observations never modified)
- apps/api/app/cycle/normalization_definitions.py (AlignedValue + required `effective_period_end: date` provenance field with the Part 0/DEC-015 docstring; CURRENT_MODEL_VERSION bumped → `normalization-v0.2` / method `part0-period-complete-alignment-r1` — alignment semantics changed, so a new version, never a silent edit)
- apps/api/scripts/normalize_smoke.py (no-signal message wording: period-complete eligibility)
- apps/api/tests/test_normalization_signals.py (+5 period-completeness regressions; former 2023-Q2 leak expectation inverted; 2024-Q4 full-freshness test annotated as the year-end eligibility boundary — 27 → 32 tests)
- .dev/NORMALIZATION.md (+ "Part 0 implementation status" section; §3 rewritten: eligibility rule, effective_period_end in the AlignedValue structure, period-completeness ≠ release-date safety; Sprint 5.6 status block marked superseded)
- .dev/DECISIONS.md (+DEC-013 decay shape confirmed exponential, +DEC-014 Sprint 5.7 direction = WGI momentum, +DEC-015 period-complete alignment)
- .dev status files (HANDOFF, PROJECT_STATUS, DEVLOG, BACKLOG)

No frontend, API-route, model, migration, mapping, seed, or raw-data changes. No npm build run (no frontend files touched). Live verification read-only: CHE RULE_OF_LAW_WGI_SCORE @2024-Q2 → 2023 (87.8079, the leak fixed), @2024-Q4 → 2024 (87.3184, freshness 1.0000, first eligible quarter), @2025-Q2 → 2024 unchanged (87.3184, freshness 0.9170); CREDIT_TO_GDP_GAP @2025-Q1/Q2/Q3 → own quarter via a one-off read-only alignment check on live Postgres (script deleted after use).

## Files changed in earlier session (Sprint 5.6, first executable normalization path)

- apps/api/app/services/alignment_service.py (NEW — parse_scoring_period, quarter_end_date, align_observation_as_of → AlignedValue: latest-vintage latest-prior observation with country+indicator scoping at the country lookup, latest-vintage subquery, and outer query (ISSUE-004); source_period labels by freshness class; age_periods in scoring-clock quarters; read-only)
- apps/api/app/cycle/freshness.py (NEW — FreshnessDecayShape (exponential default + linear), FreshnessPolicy with threshold-ordering validation, FreshnessResult, evaluate_freshness (full window → 1.0; decay; unusable → 0.0 + is_usable False), own_period_age conversion, DEFAULT_FRESHNESS_POLICIES built from the Sprint 5.5 FRESHNESS_THRESHOLDS)
- apps/api/app/cycle/normalizer.py (NEW — normalize_indicator_as_of dispatcher: DIRECT_0_100 only; NormalizationNotImplementedError for every other family; NormalizationDataError on out-of-range provider values (no silent clamp); rejects model configs claiming backtest_safe=True until Milestone 9; relative/momentum/confidence/reference_universe_id stay None; freshness_factor/is_stale carried as provenance)
- apps/api/app/cycle/normalization_definitions.py (NormalizedSignal + freshness_factor/is_stale provenance fields with 0..1 validation; CURRENT_MODEL_VERSION → version_id `normalization-v0.1`, method `sprint-5.6-direct-0-100-r1`, backtest_safe False)
- apps/api/scripts/normalize_smoke.py (NEW — read-only live-DB smoke: --country --indicator --period; prints source observation, raw value, level score, not-calculated dimensions, freshness factor, backtest-safe, model version; clear exit-1 messages for no-signal/unsupported/unknown)
- apps/api/tests/test_normalization_signals.py (NEW — 27 offline tests covering all 19 sprint-required areas + biennial-gap + policy-validation + half-life cases)
- .dev/NORMALIZATION.md (+ "Sprint 5.6 implementation status" section; §17.1 note — machinery now executes with the §5 thresholds and an exponential initial default, shape still a versioned open question)
- .dev status files (HANDOFF, PROJECT_STATUS, DEVLOG, BACKLOG)

No frontend, API-route, model, migration, mapping, seed, or raw-data changes. No npm build run (no frontend files touched). Smoke script verified live (read-only): CHE RULE_OF_LAW_WGI_SCORE @2025-Q2 → 2024 obs, 87.3184, freshness 0.9170; USA CONTROL_OF_CORRUPTION_WGI_SCORE @2024-Q4 → 69.8552, freshness 1.0000; 1990-Q1 and non-DIRECT_0_100 indicators exit cleanly.

### Pending owner actions (2026-09-09, after Sprint 5.9)

- Nothing blocking. Dev processes remain down (no frontend work this sprint); restart them in your own terminals when browser work resumes.
- Decide Sprint 5.10. Recommended per DEC-018: DSR OWN_HISTORY level design + implementation (READY_FOR_IMPLEMENTATION_DESIGN — self-contained, no external calibration data needed; design questions listed in NORMALIZATION.md Sprint 5.9 audit). Alternatives: confidence composition (§17.6) or a data-side sprint (education Option C / IMF WEO / WID). NOTE: the pre-audit Sprint 5.9 recommendation (TARGET_BAND/MONOTONIC parameter design) is now largely MOOT — Sprint 5.9 reclassified the TARGET_BAND/MONOTONIC proposals to CONTEXTUAL_DEFERRED; only the credit-gap asymmetric TARGET_BAND remains, and its thresholds still need documented external evidence (BIS early-warning literature) first.
- Open model-version questions (nothing executable depends on them yet): the tracked_8 membership, complete-universe rule, and mid-rank formula are versioned in normalization-v0.4 — any change (including adding a country to the universe) is a v0.5 + new universe id, never a silent edit; relative scores for other CROSS_SECTIONAL_RELATIVE indicators need their level families approved first; momentum for non-WGI indicators needs per-indicator sign conventions (§17 item 5, open for non-WGI indicators).

## Files changed in earlier session (Sprint 5.5, normalization methodology & architecture design)

- .dev/NORMALIZATION.md (NEW — methodology source of truth: score semantics / quarterly clock / as-of alignment / backtest-safety limitation / freshness policy / 9 normalization families / per-indicator classification for all 19 live series / absolute-vs-relative / momentum windows / confidence architecture / MISSING ≠ ZERO / ceiling behavior / versioning / tracked_8 universe / calibration-vs-as-of windows / robust statistics / force-score eligibility / 11 unresolved questions in §17)
- apps/api/app/cycle/normalization_definitions.py (NEW — typed skeleton + registry: NormalizationFamily enum, ScoringPeriod, FreshnessClass/FreshnessThresholds, NormalizationSpec, AlignedValue, NormalizedSignal with unscored() + range validation, ModelVersionConfig with tracked_8 reference universes, LIVE_INDICATOR_CODES, NORMALIZATION_REGISTRY for all 19 indicators, import-time validate_normalization_registry(); score fields Optional/None — nothing computed, nothing persisted, no API connection)
- apps/api/tests/test_normalization.py (NEW — 19 offline unit tests)
- .dev/DECISIONS.md (+DEC-012 — four separable dimensions, quarterly clock, no zero-fill, explicit families, versioned calibration/universe, not backtest-safe)
- .dev status files (HANDOFF, PROJECT_STATUS, DEVLOG, BACKLOG)

No frontend, API-route, model, migration, mapping, seed, or raw-data changes. No npm build run (no frontend files touched).

## Files changed in earlier session (Milestone 5.4, military-expenditure proxy + normalization-readiness audit)

- apps/api/app/data_sources/world_bank_mappings.py (+2 verified mappings: MILITARY_EXPENDITURE_USD→MS.MIL.XPND.CD, MILITARY_EXPENDITURE_GDP→MS.MIL.XPND.GD.ZS → 15 total; transform notes name the SIPRI underlying source and the input-not-capability caveat)
- apps/api/app/db/seed.py (+2 canonical indicators: Military category, annual, strength_direction contextual, SIPRI-attributed descriptions; catalog 23→25, series 17→19)
- apps/api/app/cycle/force_definitions.py (military_strength promoted to live = MILITARY_EXPENDITURE_USD + MILITARY_EXPENDITURE_GDP with `coverage_ceiling = partial` and a SIPRI-input-measure coverage note; no other forces touched)
- apps/api/tests/test_military.py (NEW — 11 offline tests)
- apps/api/tests/test_world_bank_mappings.py, test_world_bank_trade.py, test_gini_cpi.py, test_wgi_governance.py, test_oecd_persistence.py, test_bis_persistence.py, test_indicators.py (baseline updates: WB mappings 15, series 19, catalog 25; EXPECTED code dicts +2), test_force_coverage.py (military_strength missing→defined_not_sourced exemplar)
- apps/web/app/country/[iso3]/page.tsx (+compact Military section: latest expenditure in compact current US$ + % of GDP, year, "Source: World Bank · Underlying source: SIPRI", input-proxy caption, no history/ranking/score; renders only when military data exists; /forces pages needed no changes — already generic)
- .dev/DECISIONS.md (+DEC-011 military spending proxy via WB-republished SIPRI series, capped at PARTIAL)
- .dev/FORCE_COVERAGE.md (military row + ceiling section + per-country tables 7/3/1/6 + 5/5/1/6; NEW "Normalization Readiness" section — 19-series audit matrix + 7 findings for Sprint 5.5)
- .dev status files (HANDOFF, PROJECT_STATUS, DEVLOG, DATA_SOURCES, BACKLOG)

### Pending owner actions (2026-09-08, after Milestone 5.4)

- Nothing blocking. If the Claude session that closed 5.4 has ended, restart both dev processes in your own terminals (backend: `uv run --no-sync python run.py` from apps/api; web: `npm run dev -w apps/web` from the repo root) and verify http://localhost:3000/country/CHE, /country/USA, /country/CHE/forces, /country/CHN/forces.

## Files changed in earlier session (Milestone 5.3, inequality + CPI + coverage-semantics hardening)

- apps/api/app/cycle/force_definitions.py (+`coverage_ceiling: ForceCoverageStatus | None = None` field; wealth_opportunity_values_gaps promoted to live = GINI_INDEX with ceiling + income-inequality-only note naming future inputs; internal_conflict gains the ceiling + note; cost_competitiveness live = UNIT_LABOUR_COST_GROWTH + INFLATION_CPI with the domestic-price-pressure caveat)
- apps/api/app/services/force_coverage_service.py (3-line ceiling cap in `_force_status`; API schema unchanged)
- apps/api/app/data_sources/world_bank_mappings.py (+2 verified mappings: GINI_INDEX→SI.POV.GINI, INFLATION_CPI→FP.CPI.TOTL.ZG → 13 total)
- apps/api/app/db/seed.py (+GINI_INDEX canonical entry — Inequality, index (0-100), irregular, negative; INFLATION_CPI frequency corrected monthly → annual with a dated description; catalog 22→23, series 15→17)
- apps/api/tests/test_gini_cpi.py (NEW — 14 offline tests)
- apps/api/tests/test_world_bank_mappings.py, test_world_bank_trade.py, test_oecd_persistence.py, test_bis_persistence.py, test_indicators.py, test_wgi_governance.py, test_force_coverage.py (baseline updates: mappings 13, series 17, catalog 23; internal-conflict ceiling test; cost-competitiveness live set)
- apps/web/app/country/[iso3]/page.tsx (+compact Inequality section: latest Gini, "Latest available: {year}", Source: World Bank, income-inequality-only caption; /forces pages needed no changes — already generic)
- .dev/DECISIONS.md (+DEC-009 coverage ceiling, +DEC-010 CPI frequency correction)
- .dev status files (HANDOFF, PROJECT_STATUS, DEVLOG, DATA_SOURCES, FORCE_COVERAGE, BACKLOG)

## Files changed in earlier session (Milestone 5.2, WGI governance expansion + route diagnosis)

- apps/api/app/data_sources/world_bank_mappings.py (+3 verified WGI mappings: RULE_OF_LAW_WGI_SCORE→GOV_WGI_RL_SC, CONTROL_OF_CORRUPTION_WGI_SCORE→GOV_WGI_CC_SC, POLITICAL_STABILITY_WGI_SCORE→GOV_WGI_PV_SC → 11 total)
- apps/api/app/db/seed.py (+3 canonical indicators: Governance category, score 0-100, annual, positive direction; catalog 19→22)
- apps/api/app/cycle/force_definitions.py (rule_of_law + corruption + internal_conflict promoted to live with WGI-scope coverage notes; no other forces touched)
- apps/api/tests/test_wgi_governance.py (NEW — 12 offline tests)
- apps/api/tests/test_world_bank_mappings.py (baseline 8→11 mappings / 12→15 series)
- apps/api/tests/test_world_bank_trade.py (EXPECTED_WB_CODES +3; series count 12→15)
- apps/api/tests/test_oecd_persistence.py (catalog 19→22, series 12→15)
- apps/api/tests/test_bis_persistence.py + tests/test_indicators.py (catalog 19→22)
- apps/api/tests/test_force_coverage.py (military_strength replaces rule_of_law as the missing-force exemplar)
- apps/web/app/country/[iso3]/page.tsx (+compact Governance section: latest WGI Rule of Law / Control of Corruption / Political Stability, XX.XX / 100, year, World Bank / WGI source, perception/uncertainty caption; no good/bad labels; renders only when governance data exists)
- .dev/DECISIONS.md (DEC-001 Impact corrected to npm; +DEC-006 WGI 2025-revision score series, +DEC-007 education Option C, +DEC-008 government debt keeps general-government)
- .dev status files (HANDOFF, PROJECT_STATUS, DEVLOG, DATA_SOURCES, FORCE_COVERAGE, BACKLOG)

## Files changed in earlier session (Milestone 5.1, WB trade & investment expansion)

- apps/api/app/data_sources/world_bank_mappings.py (+5 verified mappings: EXPORTS_GDP, IMPORTS_GDP, TRADE_BALANCE (published external balance — not derived), CURRENT_ACCOUNT_GDP, GROSS_CAPITAL_FORMATION_GDP → 8 total)
- apps/api/app/cycle/force_definitions.py (trade_capital_flows + infrastructure_investment candidates promoted to live with updated coverage notes; global_openness note updated — trade data available as future input candidate, status deliberately unchanged)
- apps/api/tests/test_world_bank_trade.py (NEW — 12 offline tests)
- apps/api/tests/test_world_bank_mappings.py (baseline 3→8 mappings / 7→12 series)
- apps/api/tests/test_oecd_persistence.py (baseline total series 7→12)
- apps/web/app/country/[iso3]/page.tsx (+compact Trade & Investment section: latest exports/imports/trade balance/current account/GCF, % of GDP, year + source labels; renders only when trade data exists)
- .dev status files (HANDOFF, PROJECT_STATUS, DEVLOG, DATA_SOURCES, FORCE_COVERAGE, BACKLOG)

## Files changed in earlier session (Milestone 5.0, 17-force coverage foundation)

- apps/api/app/cycle/__init__.py + app/cycle/force_definitions.py (NEW — 17 forces, ForceCoverageStatus enum, live/candidate indicator codes, coverage notes; unassigned-indicator rationale documented)
- apps/api/app/services/force_coverage_service.py (NEW — deterministic statuses, 3 aggregate queries, no N+1)
- apps/api/app/schemas/force_coverage.py (NEW — ForceInputRead / ForceCoverageRead / ForceCoverageResponse; no score fields)
- apps/api/app/api/routes/countries.py (+GET /{iso3}/force-coverage)
- apps/api/tests/test_force_coverage.py (NEW — 11 offline tests)
- apps/web/lib/api.ts (+ForceInput / ForceCoverage / ForceCoverageData types, +fetchForceCoverage)
- apps/web/app/country/[iso3]/forces/page.tsx (NEW — 17 forces, dynamic summary, status labels, source labels, no gauges/scores)
- apps/web/app/country/[iso3]/page.tsx (+Big Cycle Forces entry card with "View force coverage →")
- .dev/FORCE_COVERAGE.md (NEW — 17-force data-gap report + per-country coverage + top-3 next data priorities)
- .dev status files

### Pending owner actions (2026-09-08)

- **Restart the backend on :8000** — the running instance (started 20:10, PID 55064) predates the force-coverage route and its uvicorn reloader is not picking up new files; `uv run python run.py` needs a fresh start. The classifier blocked Stop-Process; owner command: `! Get-NetTCPConnection -LocalPort 8000 -State Listen | Select-Object -ExpandProperty OwningProcess -Unique | ForEach-Object { Stop-Process -Id $_ -Force }` then restart the backend.
- **`npm run build`** — still pending from Sprint 4.11–4.14; stop `next dev` first (same classifier block; same `!` command with port 3000).

## Files changed in earlier session (Sprints 4.11–4.14, OECD approval → persistence → Productivity UI)

- apps/api/app/db/seed.py (+2 owner-approved canonical indicators LABOUR_PRODUCTIVITY_PER_HOUR (Productivity) / UNIT_LABOUR_COST_GROWTH (Cost Competitiveness, with description); SourceSeries seeding generalized to WORLD_BANK_MAPPINGS + BIS_MAPPINGS + OECD_MAPPINGS — 19 indicators, 7 series, idempotent)
- apps/api/app/data_sources/base.py (+DataSourceNoDataError — the source clearly reports "no observations exist"; distinct from transport/parse failures)
- apps/api/app/data_sources/oecd.py (404 + body "NoRecordsFound" (verified live for CHN) → DataSourceNoDataError)
- apps/api/app/data_sources/oecd_mappings.py (docstring: PROPOSED → OWNER-APPROVED + seeded; LR raw-code caveat preserved)
- apps/api/app/services/ingestion_service.py (run_ingestion catches DataSourceNoDataError → zero-data SUCCESS IngestionRun with run_metadata.no_data=true — legitimate no coverage is never a failed run)
- apps/api/scripts/ingest_oecd.py (NEW — batch CLI: OECDAdapter + TransientRetryAdapter (network/5xx only, never 4xx/no-data/parse/mapping) + run_ingestion, --all-countries/--all-mapped/--start/--end, strictly sequential with a 1s inter-request delay, no-data counted separately from failures)
- apps/api/app/models/indicator.py + observation.py + source_series.py (unit/transform_notes column widths — see migrations)
- apps/api/alembic/versions/1724f2da4f3f, 1389fa4e7ce0, 38fff2cf97c9 (indicators.unit 20→100; source_series.transform_notes 500→2000; observations.unit 20→100)
- apps/api/tests/test_oecd_persistence.py (NEW — 12 offline tests: seeding 19/7 + idempotency, annual + quarterly persistence, duplicate skip, run_ingestion happy path, no-data (404 NoRecordsFound + empty 200), source=oecd filtering, source+indicator combinations, country isolation, latest-vintage)
- apps/api/tests/test_oecd_adapter.py (404 test split: plain 404 stays DataSourceHTTPError; 404 NoRecordsFound → DataSourceNoDataError)
- apps/api/tests/test_indicators.py (catalog count 17→19), tests/test_world_bank_mappings.py (series 5→7), tests/test_bis_persistence.py (catalog 17→19)
- apps/web/app/country/[iso3]/page.tsx (SOURCE_LABELS +oecd; Productivity & Competitiveness section: latest labour productivity + ULC growth cards, 8-row annual productivity history, 8-quarter ULC history, source labels, CHN/IND safe empty state)
- .dev status files

## Files changed in earlier session (Sprints 4.8–4.10, OECD discovery → adapter → live smoke)

- apps/api/app/data_sources/oecd_mappings.py (NEW — OecdMapping registry: agency/dataflow/version/{cc} key templates with REF_AREA-first order, 2 indicators, expected_dims per dataflow, OECD_REF_AREA_BY_ISO3 identity mapping; docstring records verified API findings + coverage)
- apps/api/app/data_sources/oecd.py (NEW — OECDAdapter: v1 exact-dot-key URL `GET /data/{agency},{dataflow},{version}/{key}?format=csvfilewithlabels`, annual/quarterly period parsing, per-row REF_AREA + dimension validation, null skip, OBS_STATUS preserved in raw_payload, release_date None)
- apps/api/tests/test_oecd_adapter.py (NEW — 14 offline MockTransport tests)
- apps/api/scripts/oecd_smoke.py (NEW — live read-only smoke, no DB writes)
- data_sources table: OECD status planned → testing (live fetch succeeded)
- .dev status files

## Files changed in earlier sessions (Sprints 4.1–4.7 BIS end-to-end; Sprints 3.9–3.12 WB batch/coverage)

- See git status and DEVLOG — BIS adapter/mappings/persistence/UI, WB batch retry, coverage aggregation, cross-country leak fix (ISSUE-004), uq_observations_identity.

### Environment notes (2026-09-08)

- Dev DB: host PostgreSQL 18 (`bigcycleatlas_dev` / `bigcycleatlas_app`, creds in apps/api/.env).
- On Windows, any script using async psycopg must set `WindowsSelectorEventLoopPolicy` before `asyncio.run` (run.py does; ingest CLIs do; app.db.seed does not — run it via a policy-setting wrapper as done in Sprint 4.11–4.14).
- **uvicorn 0.36+ quirk:** running `uvicorn app.main:app` directly (no reload) ignores the event-loop policy via its explicit loop_factory → ProactorEventLoop → async psycopg fails. Use `python run.py` (reload mode) to run the backend.
- `uv run` outside the venv resolves the global Python 3.13 — use `uv run --no-sync` or `.venv/Scripts/*.exe` for alembic/pytest.
- `uv sync` needs `--system-certs` in this environment (PyPI TLS).
- psql auth fails when the password contains characters the shell mangles; prefer the app's own session layer (`scripts/coverage_report.py`) for DB inspection.

(Earlier sessions: Milestone 1 frontend refactor and Milestone 2 models/services/routes — see git status and DEVLOG.)

## Important decisions

- DEC-001: npm workspaces chosen as package manager (pnpm not installed)
- DEC-002: SQLAlchemy 2.0 async + psycopg (not asyncpg) for Postgres
- DEC-003: SQLite used for unit tests via dependency override; Postgres for real runs
- DEC-004: packages/shared is the source of truth for the 8 initial countries
- Observation identity uniqueness enforced at DB level (uq_observations_identity, migration a1c7e9b04d55) after a concurrent-batch duplicate incident — an engineering fix with NO DECISIONS.md record (DEC-005 in DECISIONS.md is the frontend-fetch decision; the old HANDOFF/DEVLOG "DEC-005" label was a numbering mistake, corrected here 2026-09-09)
- A source-reported "no observations exist" (e.g. OECD 404 NoRecordsFound) is a clean no-data outcome (DataSourceNoDataError → zero-data success IngestionRun), never a failed run — known no-coverage countries are not outages — likewise NO DECISIONS.md record (DEC-006 in DECISIONS.md is the WGI 2025-revision decision)
- WGI governance (DECISIONS.md DEC-006, 2026-09-08): the current WDI 2025-revision 0–100 governance-score series (GOV_WGI_*_SC) are the source indicators for Rule of law / Corruption / Internal conflict — legacy RL.EST/CC.EST/PV.EST no longer exist in WDI; WGI values are source indicators, never Atlas force scores
- Education (DECISIONS.md DEC-007, 2026-09-08): Option C — keep the age-specific/attainment concepts, reject WB gross enrollment ratios; better indicators come later
- Government debt (DECISIONS.md DEC-008, 2026-09-08): keep the general-government concept, reject WB central-government debt as a proxy; seek a genuine source (e.g. IMF WEO)
- Coverage ceiling (DECISIONS.md DEC-009, 2026-09-08): proxy-only forces may declare `coverage_ceiling = partial` — complete data still reports PARTIAL, never AVAILABLE; applied to Wealth/opportunity/values gaps (Gini), Internal conflict (WGI proxy), and Military strength (spending proxy, 5.4); internal conflict's reclassification is a methodology correction; lifting a ceiling is a deliberate future owner decision
- CPI frequency (DECISIONS.md DEC-010, 2026-09-08): INFLATION_CPI frequency corrected monthly → annual to match the verified WB series (documented, not silent); `/api/indicators?frequency=monthly` now returns only UNEMPLOYMENT_RATE
- Military proxy (DECISIONS.md DEC-011, 2026-09-08): Military strength's initial proxy is the SIPRI-derived WDI military-expenditure pair (MS.MIL.XPND.CD / MS.MIL.XPND.GD.ZS) via the existing WB pipeline — no direct SIPRI adapter, no SIPRI workbook; both indicators contextual; the force is capped at PARTIAL (spending is an INPUT measure, never capability)
- Normalization architecture (DECISIONS.md DEC-012, 2026-09-09): four separable score dimensions, quarterly clock, no zero-fill, explicit normalization families, versioned calibration/universe, not backtest-safe until Milestone 9
- Freshness decay shape (DECISIONS.md DEC-013, 2026-09-09): exponential confirmed by owner as the settled model choice (§17.1 closed as a shape decision; parameter tuning = version change)
- Sprint 5.7 direction (DECISIONS.md DEC-014, 2026-09-09): WGI momentum (OWN_HISTORY) next; relative scores and TARGET_BAND parameter design deferred
- Period-complete alignment (DECISIONS.md DEC-015, 2026-09-09): eligibility = `effective_period_end <= scoring_period_end`, derived-layer only; fixes period-completeness leakage ONLY, never release-date safety
- WGI momentum (DECISIONS.md DEC-016, 2026-09-09): OWN_HISTORY signed raw-point change for the WGI ×3; windows (3, 5) with 5y primary; anchor tolerance 1 annual period; higher WGI = positive; missing anchor → None never 0, no fallback; anchors inherit DEC-015; WGI-scale-specific, NOT approved for cross-indicator aggregation; backtest_safe False; model version normalization-v0.3
- WGI relative scores (DECISIONS.md DEC-017, 2026-09-09): relative position within the FROZEN tracked_8 universe (ReferenceUniverseSpec, never DB-derived, changes = new id + new model version); mid-rank plotting position 100 * (average_rank − 0.5) / n, ties = average rank; NOT a global/world percentile; all 8 members usable or nobody is scored (usable/expected counts carried); freshness gates usability but never scales the score; separate from level/momentum/confidence; model version normalization-v0.4

## Questions / decisions needed from project owner

- Font choice: design.md specifies Suisse Intl (commercial). The frontend currently uses a system font stack. Should we license Suisse Intl or pick a free alternative (e.g. Inter, Neue Haas Grotesk substitute)?

## Recommended next action

Sprint 5.8 is closed and verified (278 tests, live read-only smoke covering top/bottom/mid relative positions computed from the live aligned cross-section, no frontend change); the WGI ×3 now publish three of the four separable dimensions (level + momentum + relative within tracked_8) with full provenance, model version normalization-v0.4. Recommended Sprint 5.9: the **TARGET_BAND/MONOTONIC parameter-design sprint** (§17.2–17.4, requires owner-approved curve thresholds — unlocks level signals for the debt/productivity/inequality/CPI families), or **confidence composition design** (§17.6 — the last unimplemented dimension). Data-side alternatives: education Option C (DEC-007), IMF WEO debt (DEC-008), WID (lifts the Wealth-gap PARTIAL ceiling).
