# Project Status

## Phase

Milestone 3 — COMPLETE (World Bank connector). Milestone 4 — IN PROGRESS (WB, BIS, OECD all end-to-end; remaining sources deferred). Milestone 5.0 — COMPLETE (17-force coverage foundation). Milestone 5.1 — COMPLETE (WB trade & investment expansion). Milestone 5.2 — COMPLETE (WGI governance expansion). Milestone 5.3 — COMPLETE (coverage-semantics ceiling + WB Gini + WB CPI: proxy-ceiling mechanism, 2 verified mappings, 8-country imports, wealth-gap + cost-competitiveness promotions, Inequality UI section — no scores). Milestone 5.4 — COMPLETE (SIPRI-derived WB military-expenditure proxy: 2 verified mappings, 8-country imports, Military strength PARTIAL via the DEC-009 ceiling, Military UI section, normalization-readiness audit — no scores; final data-expansion sprint before normalization). Sprint 5.5 — COMPLETE (normalization methodology & architecture design: .dev/NORMALIZATION.md spec + typed skeleton/registry, four separable score dimensions, quarterly clock, DEC-012 — no scores published). Sprint 5.6 — COMPLETE (first executable normalization path: as-of alignment + freshness machinery + DIRECT_0_100 level signals for the WGI ×3, on demand only — no persistence, no public API, no force scores; all non-WGI indicators raise NormalizationNotImplementedError). Part 0 — COMPLETE 2026-09-09 (period-complete alignment hardening, DEC-015: eligibility = effective_period_end <= scoring period end — annual first eligible at its own Q4; derived layer only, raw rows never modified; NO release-date safety, backtest_safe stays False; model version normalization-v0.2 / part0-period-complete-alignment-r1). Sprint 5.7 — COMPLETE 2026-09-09 (WGI momentum, DEC-016: OWN_HISTORY signed raw-point change for the WGI ×3; windows (3, 5) with 5y primary, anchor tolerance 1 annual period, both versioned; anchors via align_observation_as_of at the shifted period — DEC-015 inherited; per-window MomentumWindowResult provenance; missing history → None never 0, no fallback; anchor never freshness-decayed; model version normalization-v0.3 / sprint-5.7-wgi-momentum-r1; NOT approved for cross-indicator aggregation). Sprint 5.8 — COMPLETE 2026-09-09 (WGI relative scores, DEC-017: CROSS_SECTIONAL_RELATIVE mid-rank positions within the frozen typed tracked_8 universe for the WGI ×3 — 100 * (average_rank − 0.5) / n, ties = average rank, complete-universe rule 8/8-or-nobody with expected/usable provenance, freshness gates usability but never scales, NOT a global percentile; model version normalization-v0.4 / sprint-5.8-wgi-tracked8-relative-r1; the other CROSS_SECTIONAL_RELATIVE registry entries do NOT enable). Sprint 5.9 — COMPLETE 2026-09-09 (normalization parameter decision audit + relative-path hardening, DEC-018: `build_relative_cross_section` enforces its own execution gate (level DIRECT_0_100 AND relative CROSS_SECTIONAL_RELATIVE → exactly the WGI ×3 — direct helper calls for non-approved indicators raise NormalizationNotImplementedError; WGI outputs unchanged, no model-version bump); 8 non-WGI level methodologies audited — GDP_GROWTH / GROSS_CAPITAL_FORMATION_GDP / INFLATION_CPI / UNIT_LABOUR_COST_GROWTH reclassified to CONTEXTUAL_DEFERRED, Gini direction confirmed without a numeric curve, credit-gap asymmetric TARGET_BAND confirmed with thresholds unresolved, DSR OWN_HISTORY confirmed and selected as the next implementation target, productivity direction confirmed with the absolute level curve deferred; NO numeric thresholds invented). Sprint 5.10 — COMPLETE 2026-09-09 (DSR OWN_HISTORY level, DEC-019: the FIRST non-WGI level signal, for exactly DEBT_SERVICE_RATIO — expanding as-of own-history calibration of real latest-vintage observations (inclusive of the current observation, DEC-015 eligibility, no future data, no forward-fill), empirical mid-rank stress percentile `100 * (average_rank − 0.5) / n` with ties = average rank, `level_score = 100 − stress_percentile` (higher DSR = greater burden = weaker), minimum 20 observations (Atlas MODEL PARAMETER — below → None never 0), endpoints not forced to 100/0, freshness gates the current observation only and NEVER scales, typed OwnHistoryLevelResult provenance, execution gated by the versioned own_history_level_configs entry (registry OWN_HISTORY alone never auto-enables); model version normalization-v0.5 / sprint-5.10-dsr-own-history-level-r1; WGI level/momentum/relative byte-equivalent; DSR relative prohibited (BIS caution), DSR momentum not implemented, confidence None). Sprint 5.11 — COMPLETE 2026-09-09 (credit-to-GDP gap evidence audit, DEC-020: methodology only, NO score implemented — positive-side excess-credit breakpoints SUPPORTED by official evidence (Basel CCyB guide +2/+10 policy reference point; BIS 2018 EWI critical threshold ~9pp, amber 4–9 / red ≥9), negative-side penalty NOT supported (guide flat zero below +2; persistent negative gap = post-crisis phase and/or boom-contaminated-trend measurement artifact) → CREDIT_TO_GDP_GAP level family RECLASSIFIED asymmetric TARGET_BAND → ONE_SIDED_VULNERABILITY (new NormalizationFamily member), still raises NormalizationNotImplementedError, NO numeric Atlas breakpoints approved (neutral ceiling / curve shape / endpoints / no-excess-region interpretation = owner decisions); three threshold concepts kept separated — Basel policy-guide vs statistical early-warning vs Atlas curve parameters; no model-version bump (v0.5)). Sprint 5.12 — COMPLETE 2026-09-09 (credit-gap ONE_SIDED_VULNERABILITY level, DEC-021: the second non-WGI level signal, for exactly CREDIT_TO_GDP_GAP — the OWNER-APPROVED curve: flat 50 at/below +2pp (no-excess region deliberately NEUTRAL, not 100 — absence of excess credit is not evidence of strength), strictly linear from (+2, 50) to (+10, 0), clamped 0 at/above +10pp; negative gaps carry NO penalty (DEC-020 retraction preserved); breakpoints coincide with the Basel CCyB guide L/H but the 50/0 mapping is an Atlas MODEL PARAMETER; no minimum-history gate (parametric curve), freshness gates current only and never scales; execution gated by the explicit one_sided_vulnerability_configs entry (registry alone never auto-enables); relative stays CONTEXTUAL_DEFERRED, momentum windows 4q/8q stay UNAPPROVED, confidence None, backtest_safe False; model version normalization-v0.6 / sprint-5.12-credit-gap-one-sided-level-r1; Basel-caution score-interpretation limits carried). Sprint 5.13 — COMPLETE 2026-09-09 (confidence layering + WGI uncertainty audit, DEC-022: ARCHITECTURE/METHODOLOGY ONLY — no code change, pytest 318 unchanged, model version stays normalization-v0.6, confidence stays None, nothing ingested; confidence layers separated — indicator trust (§10.2) vs force completeness (§10.3) with coverage/proxy ceilings LOCKED at the force layer only, confidence never multiplies economic scores, confidence ≠ freshness (freshness_factor is the one freshness truth), missing uncertainty ≠ perfect ≠ zero, no arbitrary numeric provider-quality constants; WGI uncertainty series VERIFIED live — LB/UB are 90% CI bounds on the SAME 0–100 score scale as the imported score (bracket it for all 624 tracked_8 points, UB clamped at 100), SE is on the underlying estimate scale and empirically width ≈ 56.5×SE (NOT 65.8×SE — SE cannot reconstruct the published bounds), SR integer counts 4–16, all 8 countries × all 4 series × every score year 1996–2024 with perfect one-to-one alignment; initial input set chosen LB + UB + SR (SE deferred); storage representation chosen — dedicated indicator_diagnostics table, REQUIRES A MIGRATION so ingestion is deliberately deferred to Sprint 5.14 with the spec recorded; numeric confidence composition still open (§17 item 6 — layering resolved, formula/weights only remain)). Sprint 5.19 — COMPLETE 2026-09-10 (WEO debt implementation + Education/WID final verification: IMF WEO `GGXWDG_NGDP` IMPLEMENTED via SDMX 3.0 — 297 observations across 8 tracked_8, historical-only via `LATEST_ACTUAL_ANNUAL_DATA` filtering, idempotent re-import verified, adapter + mappings + CLI + seed + 21 offline tests; OECD tertiary attainment VERIFIED — correct dataflow `DSD_EAG_LSO_EA@DF_LSO_NEAC_DISTR_EA` (NOT `_MIGR`), age Y25T64 (NOT Y25T34), annual (NOT triennial), percentages (NOT fractions), 8/8 coverage with CHN/IND sparse, verdict EDUCATION_IMPLEMENTABLE_PARTIAL; WID wealth VERIFIED — canonical series `shwealj992` pop=`j` (NOT pop=`i`), all 8 countries 1980–2024, extrapolation counts DEU 24 / FRA 80 / GBR 93 / others 0, recommend excluding data_quality=2, verdict WID_IMPLEMENTABLE; DEC-025 corrected; no force aggregation, no force scores, no normalization, no commit/push).

## Progress

~97% — Milestones 1–3 complete, WB + BIS + OECD end-to-end, Milestone 5.0 force-coverage foundation + 5.1 trade/investment + 5.2 WGI governance + 5.3 inequality/CPI/coverage-semantics + 5.4 military proxy + Sprint 5.5 normalization design + Sprint 5.6 first executable normalization path (DIRECT_0_100 for the WGI ×3) + Part 0 period-complete alignment hardening (DEC-015) + Sprint 5.7 WGI momentum (DEC-016) + Sprint 5.8 WGI relative scores on tracked_8 (DEC-017 — level + relative + momentum now executable for the WGI ×3) + Sprint 5.9 normalization parameter audit + relative hardening (DEC-018 — no new scores; 4 non-WGI level families reclassified to CONTEXTUAL_DEFERRED; DSR selected as the next implementation target) + Sprint 5.10 DSR OWN_HISTORY level (DEC-019 — the first non-WGI level signal; normalization-v0.5) + Sprint 5.11 credit-gap evidence audit (DEC-020 — family reclassified to ONE_SIDED_VULNERABILITY, no score, no thresholds) + Sprint 5.12 credit-gap ONE_SIDED_VULNERABILITY level (DEC-021 — the owner-approved curve implemented; normalization-v0.6) + Sprint 5.13 confidence layering + WGI uncertainty audit (DEC-022 — architecture only, no code change, no version bump) + Sprint 5.14 indicator diagnostics storage/persistence/lookup foundation (DEC-022 recommendation implemented; migration applied; nothing ingested) + Sprint 5.15 WGI diagnostic live ingestion (1872 indicator_diagnostics rows: 8 countries × 3 WGI indicators × LB/UB/SR × 26 score years; live-fetch path + batch CLI + IngestionRun; nothing else changed) + Sprint 5.15.1 diagnostic ingestion failure-audit hardening (SAVEPOINT around persistence; connection_invalidated propagation) + Sprint 5.16 WGI confidence calibration audit + diagnostic hardening (DEC-023 — verdict DEFER_NUMERIC_CONFIDENCE: empirical profile of 624 rows found CI width/SR redundancy Pearson -0.83, dimension differences substantial, temporal trends leaky/drifting, no external calibration basis; transaction hardening for connection_invalidated DBAPIError; no numeric confidence implemented, model version stays normalization-v0.6) + Sprint 5.17 Gini calibration universe + comparability audit (DEC-024 — verdict DEFER_GINI_LEVEL: WB SI.POV.GINI mixes income-based and consumption-based surveys without per-observation welfare-concept metadata; tracked_8 25.5-43.7 rejected as calibration universe vs world 20.2-71.1/171 countries; global distribution shifts by decade; no defensible midpoint; no Gini level_score implemented, model version stays normalization-v0.6) + Sprint 5.17.1 Gini global-universe filter hardening (the read-only profile economy/aggregate filter now uses the AUTHORITATIVE WB country-metadata endpoint region.id != NA instead of a hand-written blacklist that wrongly listed ZAF and PSE as aggregates; 217 real economies identified, ZAF 7 obs / PSE 9 obs RETAINED; corrected counts: 2430 country-year obs, 171 countries, 1963–2025, pooled 20.2–71.1 median 35.3; DEC-024 verdict UNCHANGED; pytest 379 = 373 + 6 new economy-filter regressions) + Sprint 5.18 High-Value Data Gap Closure Audit (DEC-025 — read-only research on IMF WEO debt GGXWDG_NGDP, WB SE.SEC.NENR, OECD tertiary attainment, WID wealth shweal; no ingestion, no code changes; WEO debt READY, Education PARTIAL, WID wealth READY as partial proxy; recommended sequence 5.19–5.22) done. Live data: 5647 observations — World Bank 2953 + BIS 1664 + OECD 1030 — plus 1872 auxiliary indicator diagnostics. 25 canonical indicators, 19 SourceSeries (15 WB). Force coverage (live DB): CHE/USA/DEU/FRA/GBR/JPN → 7 forces available, 3 partial (wealth gaps + internal conflict + military strength, all by the DEC-009 proxy ceiling), 1 defined_not_sourced, 6 missing; CHN/IND → 5 available, 5 partial (productivity, cost competitiveness, wealth gaps, internal conflict, military strength), 1 defined_not_sourced, 6 missing. Sprint 5.19 — COMPLETE 2026-09-10 (IMF WEO debt IMPLEMENTED via SDMX 3.0: 297 observations across 8 tracked_8, historical-only, idempotent; OECD tertiary attainment VERIFIED with corrections to DEC-025; WID wealth VERIFIED with corrections to DEC-025; no force aggregation, no force scores, no normalization, no commit/push).

## Working

- FastAPI backend: /health, /api/countries (with coverage fields), /api/indicators, /api/data/sources, /api/data/ingestion-runs
- Milestone 2 data model (indicators, observations, data sources, ingestion runs, source series) with Alembic migrations
- Ingestion run tracking (list/get endpoints; runs created by the ingestion service)
- Ingestion service (`run_ingestion`: fetch + persist + IngestionRun in one caller-owned transaction) with batch CLIs (`scripts/ingest_world_bank.py`, `scripts/ingest_bis.py`, `scripts/ingest_oecd.py`)
- Batch importers with transient retry (max 3 attempts, network/5xx only) and per-series failure isolation + batch summary; OECD batch is sequential with a 1-second inter-request delay (documented ~60 queries/hour rate limit; HTTP 4xx never retried)
- No-data handling: OECD 404 `NoRecordsFound` → `DataSourceNoDataError` → zero-data SUCCESS IngestionRun with `no_data=true` metadata — known no-coverage countries (CHN/IND for OECD) are never failed runs
- Revision handling: changed values create immutable new vintages + IndicatorRevision rows (latest-vintage reads everywhere)
- Observation identity unique constraint (country, source_series, period, vintage) — concurrent imports cannot duplicate
- Live data: WB GDP_GROWTH/GDP_CURRENT_USD/GDP_PER_CAPITA 2015–2025 (264 obs) + WB EXPORTS_GDP/IMPORTS_GDP/TRADE_BALANCE/CURRENT_ACCOUNT_GDP/GROSS_CAPITAL_FORMATION_GDP 2000–2025 (1031 obs; only legitimate gaps are missing 2025 values for USA/JPN exports/imports/external-balance/GCF and CHN GCF) + WB WGI RULE_OF_LAW_WGI_SCORE/CONTROL_OF_CORRUPTION_WGI_SCORE/POLITICAL_STABILITY_WGI_SCORE 1996–2024 (624 obs; 1997/1999/2001 absent — WGI biennial years; 2025-revision 0–100 scores, all 8 countries) + WB GINI_INDEX 1990–2025 request range (187 obs, irregular years preserved, latest year varies by country 2020–2024, never forward-filled) + WB INFLATION_CPI 1990–2025 (287 obs; USA through 2024, others 2025) + WB MILITARY_EXPENDITURE_USD/MILITARY_EXPENDITURE_GDP 1990–2024 (560 obs, complete 35-year histories for all 8 countries; SIPRI-derived via WDI, DEC-011) + BIS CREDIT_TO_GDP_GAP/DEBT_SERVICE_RATIO 2000 Q1–2025 Q4 (1664 obs) + OECD LABOUR_PRODUCTIVITY_PER_HOUR/UNIT_LABOUR_COST_GROWTH (1030 obs, 1990–2025 request range, actual earliest coverage varies per country); all vintage 1; idempotent re-imports verified for all sources (5.4 military: 0 inserted / all skipped / 0 revised; 0 duplicate identity groups)
- `GET /api/countries/{iso3}/observations` (latest vintage only) with `?indicator=`, `?source=` (world_bank / bis / oecd; filter applied to subquery + outer + count), and `?start=`/`?end=` period bounds (YYYY or YYYY-MM-DD, end-exclusive); every observation carries `source_key` provenance
- `GET /api/countries` enriched with observation_count / indicator_count / latest_observation_year (single aggregate query) — updates generically as new sources are imported
- /country/[iso3]: Economic data (WB indicators) + Trade & Investment section (WB: latest exports/imports/trade balance/current account/gross capital formation, % of GDP, year + source labels; new in 5.1) + Governance section (WB WGI: latest Rule of Law / Control of Corruption / Political Stability, XX.XX / 100, year, World Bank / WGI source, perception/uncertainty caption; new in 5.2) + Inequality section (WB: latest Gini index with "Latest available: {year}" staleness label — Gini is irregular — and the income-inequality-only caption; new in 5.3) + Military section (WB-republished SIPRI: latest military expenditure in compact current US$ and % of GDP, year, "Source: World Bank · Underlying source: SIPRI", input-proxy caption, no history/ranking/scores; new in 5.4) + Debt & credit section (BIS: latest gap/DSR, 8-quarter histories, quarter labels, source labels) + Productivity & Competitiveness section (OECD: latest labour productivity USD PPP/hour + ULC growth % p.a., 8-row annual productivity history, 8-quarter ULC history, source labels; CHN/IND safe empty state, never zeros)
- /countries + homepage cards: data coverage display (provider-neutral "Economic data / N indicators"; 7 indicators for the 6 OECD-covered countries, 5 for CHN/IND — generic, nothing hardcoded)
- Next.js frontend: home, /countries, /country/[iso3] pages
- pytest suite (379 passing — routes + adapter/mappings + persistence + revisions + ingestion service + batch retry + coverage + country-scoping + BIS adapter + BIS seeding/quarterly-persistence/source-filtering regression + OECD adapter/mappings/no-data + OECD seeding/annual+quarterly persistence/run-ingestion/no-data/source-filtering/isolation/latest-vintage + force definitions/mappings/coverage/API/no-score-fields + WB trade mappings/SourceSeries seeding/idempotency/trade persistence/force promotions/global-openness-stays-missing/scoping (M5.1) + WGI revised-code mappings/seeded indicators/idempotency/persistence/promotions/positive-direction semantics/scoping/no-score-fields/17-forces API (M5.2) + Gini/CPI mappings/seeding/idempotency/irregular-year persistence/proxy-ceiling behaviour/wealth-gap-partial-never-available/internal-conflict ceiling/OECD-6 cost-competitiveness available/CHN cost-competitiveness partial-with-CPI/scoping/17-forces API/no-score-fields (M5.3) + military mappings/seeding/idempotency/raw persistence/military-PARTIAL/never-AVAILABLE-from-spending/ceiling-set/scoping/17-forces API/no-score-fields (M5.4) + normalization-design registry integrity/duplicate codes/live-force-input coverage/WGI direct-0-100/DSR own-history/Gini monotonic-negative/military contextual-deferred/MISSING-≠-ZERO types/score ranges/scoring-period validation/ceiling-at-force-layer-only/no-force-score publication (S5.5) + alignment quarter-parsing/annual+quarterly as-of alignment/latest-vintage/country isolation/no future leakage/pre-first-observation → None/freshness bounds+half-life+stale-unusable/DIRECT_0_100 identity/no clamping/WGI ×3 dispatch/unsupported-family raises/relative-momentum-confidence None/backtest-safe False + config guard/no DB writes/biennial gap (S5.6) + period-complete eligibility: annual-incomplete-year never eligible before its own Q4/annual fallback to latest complete year/quarterly own-quarter eligibility/effective_period_end provenance/raw period_start never mutated/no raw rows added (Part 0) + momentum: shift_scoring_period_years semantics/v0.3 config shape (windows (3,5), primary 5, tolerance 1)/primary-outside-windows rejected/MomentumWindowResult validation/exact signed change with provenance/rising positive + falling negative/zero change is real zero/primary-None with 3y visible/1-year WGI gap accepted/beyond-tolerance rejected never stretched/period-complete anchor semantics/latest-vintage anchor/country isolation/no future anchor/anchor never freshness-decayed/out-of-range anchor raises/momentum from aligned raw not level_score (S5.7) + relative: frozen typed tracked_8 universe cross-checked against packages/shared canonical constant/universe never derived from DB/full n=8 mid-rank score table/highest 93.75 lowest 6.25/average-rank ties (pure + DB)/order independence/higher = stronger/same-snapshot alignment/historical-leakage guard/period-complete member/latest-vintage member/country isolation/complete-universe 8/8-or-nobody/missing member never zero/freshness gates usability never scales/stale-but-usable participates/out-of-range member raises/level + momentum unchanged/relative exists when momentum None/confidence None/universe provenance/non-WGI CROSS_SECTIONAL_RELATIVE stays unimplemented/backtest-safe False + v0.4/no DB writes/no force scores (S5.8) + relative-helper hardening: direct build_relative_cross_section rejects GINI_INDEX/GDP_GROWTH/GCF/productivity and contextual_deferred-relative indicators/WGI direct-helper output unchanged/complete-universe rule unchanged/read-only + post-audit registry classifications exact for all 8 audited indicators/reclassified indicators still raise (S5.9) + DSR own-history level: execution gate (explicit own_history_level_configs required — registry alone never auto-enables; a config-less model version raises for DSR; min-sample-n must be positive)/no auto-enable/expanding calibration (sample grows with the snapshot)/future-observation zero effect (an extreme 2008-Q1 value added after a 2005-Q2 score changes nothing — no look-ahead)/latest vintage used inside the calibration sample exactly once (a revised period scores against the revised value; a duplicate period is a revision, not a new observation)/country isolation (CHE history never sees DEU rows)/missing quarter stays a gap (exact-score proof that no zero-fill happened — a phantom zero would change the score)/n=19 → signal with raw + provenance but level None never 0/n=20 → first score/pure mid-rank function (average-rank ties, permutation-independence, constant history → exactly 50, empty-sample raises, current-not-in-sample raises)/lowest → 97.5 not 100/highest → 2.5 not 0/median tie → exactly 50/exact formula/OwnHistoryLevelResult validation (zero-masquerading raises; rank without score raises)/unusable staleness → no signal/freshness never scales the score/calibration points never decayed/relative + momentum + confidence stay None/backtest_safe False/v0.5 + exactly-DSR config/WGI level+momentum+relative unchanged with the DSR machinery present (CHE RL revision regression intact)/no DB writes + raw values never mutated/no force/weight/phase fields exist on NormalizedSignal (S5.10) + credit-gap reclassification: CREDIT_TO_GDP_GAP exact family = one_sided_vulnerability (reclassified from target_band by DEC-020) (S5.11) + credit-gap one-sided level: owner-table exact regression (all 8 curve points through the full align → freshness → curve path: −20/0/+2 → 50, +4 → 37.5, +6 → 25, +9 → 6.25, +10/+20 → 0)/pure-curve clamps (deep negatives → 50, above +10 → 0) + continuity + config validation (saturation must exceed the ceiling; stress must not fall above the ceiling)/execution gate (a model version without the one_sided_vulnerability_configs entry raises "not auto-enabled")/no-observation → None/too-stale → None/freshness decays but never scales/relative + momentum + confidence stay None + curve provenance travels/v0.6 + exactly-CREDIT_TO_GDP_GAP config (S5.12), all offline)
- Coverage ceiling mechanism (M5.3, DEC-009): ForceDefinition.coverage_ceiling (default None) caps a proxy-only force at PARTIAL even with complete data; applied to wealth_opportunity_values_gaps (Gini = income inequality only), internal_conflict (WGI proxy), and military_strength (spending proxy, M5.4/DEC-011); internal_conflict's AVAILABLE → PARTIAL change is a documented methodology correction
- Normalization-readiness audit (M5.4, `.dev/FORCE_COVERAGE.md`): per-indicator inventory of direction/frequency/source/comparability/transformation needs/freshness/ceiling impact for all 19 live series — audit only, no formulas designed
- Data source adapter foundation (base contract, ObservationDTO, World Bank adapter HTTP→DTO with canonical indicator mapping)
- BIS adapter + persistence (`app/data_sources/bis.py`, `bis_mappings.py`, `scripts/ingest_bis.py`): official BIS Stats API v1 (SDMX-CSV), row-level dimension validation, quarter-start observation_date, `{cc}` country-independent SourceSeries identity, IngestionRun per country × indicator (status: testing)
- OECD adapter + persistence (`app/data_sources/oecd.py`, `oecd_mappings.py`, `scripts/ingest_oecd.py`): official OECD SDMX REST API (v1-style exact dot-keys; v2 c[] filters broken, DF_PDB_LV broken — both discovered and documented), ISO3 REF_AREA-first keys, annual + quarterly periods, row-level dimension validation, 404 NoRecordsFound → clean no-data, 2 owner-approved + seeded indicators, 2 `{cc}` SourceSeries (status: testing)
- 17-force coverage foundation (Milestone 5.0): `app/cycle/force_definitions.py` (17 forces, wording per docs/data-model.md + packages/shared; typed config layer, no DB tables yet) + `app/services/force_coverage_service.py` (deterministic available/partial/defined_not_sourced/missing; 3 aggregate queries, no N+1) + `GET /api/countries/{iso3}/force-coverage` (statuses only — no scores/phases) + `/country/[iso3]/forces` page with dynamic coverage summary + country-page entry card + `.dev/FORCE_COVERAGE.md` gap report with top-3 next data priorities
- Normalization methodology & architecture (Sprint 5.5, DEC-012): `.dev/NORMALIZATION.md` source-of-truth spec (four separable dimensions — level/relative/momentum/confidence; quarterly scoring clock; as-of alignment with provenance; explicit not-backtest-safe limitation until M9; frequency-aware freshness MODEL PARAMETERS; 9 normalization families; all-19-indicator classification; absolute-vs-relative independence; momentum windows; confidence factor composition; MISSING ≠ ZERO; ceilings act at force layer only; ModelVersionConfig with tracked_8 universe + calibration-window discipline) + typed skeleton `app/cycle/normalization_definitions.py` (NormalizationFamily enum; NormalizationSpec/AlignedValue/NormalizedSignal/ScoringPeriod/FreshnessThresholds/ModelVersionConfig dataclasses; score fields Optional/None — no scores published; registry validates at import, fails loudly on duplicate codes / missing live-force-input specs / non-live entries) — not connected to the API
- First executable normalization path (Sprint 5.6, hardened by Part 0/DEC-015): `app/services/alignment_service.py` (quarter-end as-of dates, latest-vintage latest-prior selection, country+indicator scoped at every query layer, AlignedValue with age in scoring-clock quarters — read-only, no forward-fill; **period-complete eligibility**: effective_period_end (annual/irregular → YYYY-12-31, quarterly → quarter end) must be at or before the scoring period end — an annual observation first becomes eligible at its own Q4 snapshot; derived-layer only, raw rows never modified, no synthetic dates persisted) + `app/cycle/freshness.py` (FreshnessPolicy/FreshnessResult/evaluate_freshness on the §5 MODEL PARAMETERS; decay shape parameterized, exponential confirmed by owner DEC-013; too-stale → no signal, never zero) + `app/cycle/normalizer.py` (normalize_indicator_as_of; DIRECT_0_100 only — the WGI ×3 level signals equal their validated raw 0–100 values; out-of-range raises, never clamps; other families raise NormalizationNotImplementedError; relative/momentum/confidence stay None; backtest_safe False and configs claiming True rejected; model version normalization-v0.2 / part0-period-complete-alignment-r1) + read-only `scripts/normalize_smoke.py`; computed on demand, nothing persisted, no HTTP endpoint; backtest_safe stays False — period-completeness fixes period-in-progress leakage only, never release-date safety (Milestone 9)
- WGI momentum (Sprint 5.7, DEC-016): `app/services/alignment_service.py` gained `shift_scoring_period_years` (pure scoring-clock arithmetic, quarter preserved); `app/cycle/normalizer.py` computes the OWN_HISTORY signed raw-point change `momentum = current_aligned_raw − anchor_aligned_raw` for exactly the WGI ×3 (gate = level DIRECT_0_100 AND momentum OWN_HISTORY; other OWN_HISTORY registry entries do not approve momentum); anchors via align_observation_as_of at the shifted period (country isolation, latest vintage, DEC-015 eligibility, no-future-leakage inherited); windows (3, 5) both computed, headline = 5y primary ONLY (no averaging, no fallback), per-window `MomentumWindowResult` provenance; anchor tolerance 1 annual period (versioned — beyond tolerance → change None, never zero, never stretched); higher WGI = positive, no inversion; computed from aligned RAW not level_score; freshness gates the current observation only — anchors never decayed, momentum never scaled; missing history → None never 0 (level still produced); out-of-range raises never clamps; model version normalization-v0.3 / sprint-5.7-wgi-momentum-r1; WGI momentum is scale-specific, NOT approved for cross-indicator aggregation
- WGI relative scores (Sprint 5.8, DEC-017): `app/cycle/relative.py` — frozen typed `ReferenceUniverseSpec` (`tracked_8` = USA, CHN, CHE, DEU, FRA, GBR, JPN, IND exactly, NEVER derived from DB contents; a changed universe = new id + new model version) + `mid_rank_relative_scores` (pure: mid-rank plotting position 100 * (average_rank − 0.5) / n, rank 1 = weakest, rank n = strongest, ties = average rank — deterministic, order-independent) + `build_relative_cross_section` (per member `align_observation_as_of` at the SAME scoring snapshot — country isolation, latest vintage, DEC-015 period-complete eligibility, no-future-leakage all inherited; 0..100 validation, out-of-range raises NormalizationDataError); complete-universe rule — all 8 usable or relative_score = None for EVERYONE, missing members never become zero; `reference_universe_id` / `expected_n` / `usable_n` / `relative_rank` provenance on NormalizedSignal; freshness gates member usability only, NEVER scales relative_score (confidence stays None); execution gate = relative CROSS_SECTIONAL_RELATIVE AND level DIRECT_0_100 → exactly the WGI ×3 (GDP_GROWTH/GCF/Gini/labour-productivity registry entries still raise NormalizationNotImplementedError); relative independent from momentum; model version normalization-v0.4 / sprint-5.8-wgi-tracked8-relative-r1; tracked_8 relative position — NOT a global/world percentile, NOT approved for cross-indicator aggregation
- Relative-path hardening + non-WGI level audit (Sprint 5.9, DEC-018): `build_relative_cross_section` enforces its own execution gate (level DIRECT_0_100 AND relative CROSS_SECTIONAL_RELATIVE — exactly the WGI ×3 under the current registry, no WGI codes hardcoded; direct helper calls for non-approved indicators raise NormalizationNotImplementedError; WGI numerical outputs unchanged — no model-version bump, CURRENT_MODEL_VERSION stays normalization-v0.4); the 8 audited non-WGI level families per the NORMALIZATION.md "Non-WGI Level Parameter Decision Audit — Sprint 5.9" (GDP_GROWTH / GCF / INFLATION_CPI / UNIT_LABOUR_COST_GROWTH → CONTEXTUAL_DEFERRED; GINI_INDEX MONOTONIC_NEGATIVE direction confirmed with NO numeric curve — "100 − Gini" NOT approved; CREDIT_TO_GDP_GAP asymmetric TARGET_BAND confirmed, thresholds unresolved with the BIS early-warning research question recorded; DEBT_SERVICE_RATIO OWN_HISTORY confirmed and SELECTED AS THE NEXT IMPLEMENTATION TARGET (READY_FOR_IMPLEMENTATION_DESIGN); LABOUR_PRODUCTIVITY_PER_HOUR MONOTONIC_POSITIVE direction confirmed with the absolute level curve deferred — tracked_8 min-max explicitly rejected); research support: `scripts/normalization_profile.py` (READ-ONLY descriptive per-country + pooled stats for the audited indicators — no scores, no writes, not an API, NOT proof that an empirical percentile is economic truth)
- DSR OWN_HISTORY level (Sprint 5.10, DEC-019): `alignment_service.own_history_as_of` (as-of own-history selection mirroring align_observation_as_of — country isolation, indicator scoping, latest vintage per period, DEC-015 period-complete, no future data; ALL eligible rows ascending; last element IS the current observation; read-only) + `normalizer.own_history_stress_position` (pure empirical mid-rank: stress_percentile = 100 * (average_rank − 0.5) / n, rank 1 = lowest DSR = least stress, ties = average rank, membership-checked, order-independent) + `_normalize_own_history_as_of` (execution gate = explicit own_history_level_configs entry; freshness gates current only, unusable → no signal; min 20 → below, level None never 0 with counts as provenance; level = 100 − stress_percentile, no clamping, endpoints 97.5/2.5) + OwnHistoryLevelResult/OwnHistoryLevelConfig/NormalizedSignal.own_history_level provenance; dispatch refactor keeps the WGI DIRECT_0_100 path byte-equivalent; model version normalization-v0.5 / sprint-5.10-dsr-own-history-level-r1; DSR relative = None (cross-country raw-DSR ranking PROHIBITED per BIS caution), DSR momentum not implemented, confidence None; on demand, nothing persisted, no public API, no force scores
- Credit-to-GDP gap evidence audit (Sprint 5.11, DEC-020): METHODOLOGY ONLY — primary-source research (BIS Data Portal; BCBS 2010 CCyB guidance bcbs187; BIS 2014 Q&A; BIS 2018 EWI exercise, BIS QR March 2018; Basel consolidated CAD 20; supplementary IMF WP 2020/006 + BIS WP 416) established that positive-side excess-credit breakpoints are supported (policy guide +2/+10; statistical EWI ~9 / amber 4–9) while NO authoritative negative-side penalty threshold exists (guide flat zero below +2; persistent negative gap = documented boom-contaminated-trend artifact); the asymmetric TARGET_BAND family was therefore RECLASSIFIED to the new ONE_SIDED_VULNERABILITY family (no stress at or below a neutral ceiling, stress rises monotonically above; deleveraging/weak-credit belongs to other signals); full evidence matrix + tracked_8 descriptive profile in NORMALIZATION.md "Sprint 5.11"; CREDIT_TO_GDP_GAP still raises NormalizationNotImplementedError; NO numeric Atlas breakpoints approved; no model-version bump
- Credit-gap ONE_SIDED_VULNERABILITY level (Sprint 5.12, DEC-021): `normalizer.one_sided_vulnerability_level` (pure curve: flat no_excess_score at/below the neutral ceiling, strictly linear to saturated_score at the saturation value, endpoint clamps) + `_normalize_one_sided_vulnerability_as_of` (execution gate = explicit one_sided_vulnerability_configs entry — a registry one_sided_vulnerability family alone never auto-enables; align_observation_as_of → DEC-015 eligibility inherited; freshness gates current only, unusable → no signal, never zero; no minimum-history gate — the curve is parametric) + `OneSidedVulnerabilityConfig`/`OneSidedVulnerabilityResult` provenance (curve params travel with every signal) + registry validation gate (config entries require the matching registry family); owner-approved parameters (2.0, 10.0, 50.0, 0.0) versioned in the model config; the +2/+10 breakpoints coincide with the Basel CCyB guide L/H while the 50/0 mapping is an Atlas MODEL PARAMETER (three threshold concepts stay separated); no-excess region = neutral 50, never 100; negative gaps carry no penalty; model version normalization-v0.6 / sprint-5.12-credit-gap-one-sided-level-r1; relative CONTEXTUAL_DEFERRED (None), momentum windows 4q/8q UNAPPROVED (momentum None), confidence None; on demand, nothing persisted, no public API, no force scores; Basel-caution score-interpretation limits carried (common reference point, NOT a mechanical standalone rule)
- Confidence architecture (Sprint 5.13, DEC-022): METHODOLOGY ONLY — no production code changed; `.dev/NORMALIZATION.md` §10 rewritten into three parts (10.1 permanent rules — confidence independent from strength, economic scores never multiplied by confidence, confidence ≠ freshness, missing ≠ perfect ≠ zero, no arbitrary provider-quality numeric constants; 10.2 INDICATOR confidence — source_quality as qualitative/typed metadata only, freshness_factor REUSED never recomputed, optional measurement_uncertainty + method_sufficiency diagnostics, WGI same-country/dimension/source-period uncertainty alignment rule; 10.3 FORCE confidence — coverage/completeness, missing inputs, proxy completeness and DEC-009 ceilings are force-layer ONLY and never lower an individual NormalizedSignal) + the Sprint 5.13 storage decision matrix (auxiliary canonical indicators REJECTED — catalog/API/indicator_count/force-coverage pollution; multiple SourceSeries per indicator INVALID — align_observation_as_of selects by indicator and could nondeterministically return a bound instead of the score; raw_payload provider metadata FACTUALLY UNAVAILABLE — WB score-series records carry no uncertainty fields; DEDICATED indicator_diagnostics table CHOSEN — requires an Alembic migration, deliberately deferred to Sprint 5.14 with the 4-item spec recorded) + the WGI uncertainty live audit facts in NORMALIZATION.md/DATA_SOURCES.md; model version stays normalization-v0.6, confidence None everywhere, nothing ingested, no force scores

## Not built

- Other data source connectors (FRED/ALFRED, Eurostat, ECB, SNB, Comtrade)
- Education persistence (owner chose Option C — better indicators such as OECD attainment; WB gross enrollment ratios rejected, DEC-007) / government-debt persistence (owner keeps the general-government concept; WB central-government series rejected, DEC-008 — needs a non-WDI source)
- Wealth-inequality / opportunity / values indicators (WID wealth or income shares, social polarization) — the Gini-only Wealth-gap force is capped at PARTIAL (DEC-009) until these arrive; likewise ACLED-style event data for Internal conflict, and military capability data (personnel/equipment/readiness — more spending series will NOT lift the ceiling) for Military strength
- WGI uncertainty series ingestion (SC_LB/SC_UB/SE/SR — verified live 2026-09-09 in the Sprint 5.13 audit: LB/UB score-scale, SE estimate-scale, SR integer counts, perfect tracked_8 one-to-one year alignment; input set LB+UB+SR chosen, representation = dedicated indicator_diagnostics table per DEC-022; requires the Sprint 5.14 migration — deliberately NOT ingested merely because the series exist)
- 17-force scoring engine (note: 25 canonical data indicators ≠ 17 forces; no force scores/weights/phases exist). Normalization state: Sprint 5.6 implements DIRECT_0_100 level + Sprint 5.7 OWN_HISTORY momentum + Sprint 5.8 CROSS_SECTIONAL_RELATIVE relative scores on tracked_8 (DEC-017), all for the WGI ×3 only (on demand, not persisted) on the Part 0 period-complete alignment layer; Sprint 5.9 (DEC-018) hardened the relative helper's own execution gate and audited the non-WGI level families — GDP_GROWTH / GCF / INFLATION_CPI / UNIT_LABOUR_COST_GROWTH are now CONTEXTUAL_DEFERRED (universal band/saturating proposals disproved), Gini/productivity/credit-gap/DSR families confirmed without numeric curves/thresholds, DSR selected as the next implementation target; Sprint 5.10 (DEC-019) implemented the DSR OWN_HISTORY level (normalization-v0.5) — the first non-WGI level signal; DSR relative is prohibited (BIS caution) and DSR momentum is not implemented; Sprint 5.11 (DEC-020) reclassified the credit gap's level family to ONE_SIDED_VULNERABILITY (evidence-backed one-sided vulnerability mapping — no negative-side penalty); Sprint 5.12 (DEC-021) implemented the credit-gap ONE_SIDED_VULNERABILITY level with the owner-approved curve (normalization-v0.6) — the second non-WGI level signal; credit-gap relative stays CONTEXTUAL_DEFERRED and its momentum windows (4q/8q) stay unapproved; every other non-WGI indicator still raises NormalizationNotImplementedError (non-WGI CROSS_SECTIONAL_RELATIVE registry entries do NOT enable relative scores — no generic fallback); confidence remains None everywhere — Sprint 5.13 (DEC-022) resolved the confidence LAYERING (indicator trust separated from force completeness; coverage/proxy ceilings at the force layer only; no numeric confidence formula exists or is approved) so §17 item 6 is now open only for the numeric composition/weights; momentum for non-WGI indicators needs per-indicator sign conventions (§17 item 5 open for non-WGI indicators). WGI momentum and WGI tracked_8 relative position are both NOT approved for direct cross-indicator aggregation; the DSR own-history level and the credit-gap one-sided level are likewise INDICATOR-level signals, not an Indebtedness force score
- Big Cycle phase/stage calculation
- Historical charts (ECharts)
- Forecasting, backtesting, trading (all deferred per plan)

## Blocked

- Nothing. The former stale-backend blocker is RESOLVED (2026-09-08, M5.3): the live 404 was an orphaned pre-M5.0 uvicorn worker surviving Stop-Process on its reloader parent (see HANDOFF known issues); the live force-coverage route is verified (openapi.json + CHE 17 forces), the post-5.3 `npm run build` is verified SUCCESS with `next dev` stopped, and both dev processes were restarted and browser-verified.

## Current milestone

Milestone 4 in progress (WB/BIS/OECD done; remaining sources deferred behind force-gap priorities). Milestones 5.0–5.4 complete 2026-09-08; Sprint 5.5 (normalization methodology & architecture design, DEC-012), Sprint 5.6 (first executable normalization path: alignment + freshness + DIRECT_0_100 for the WGI ×3), Part 0 (period-complete alignment hardening, DEC-015, normalization-v0.2), Sprint 5.7 (WGI momentum, DEC-016, normalization-v0.3), Sprint 5.8 (WGI relative scores on tracked_8, DEC-017, normalization-v0.4), Sprint 5.9 (normalization parameter decision audit + relative-path hardening, DEC-018 — no new scores), and Sprint 5.10 (DSR OWN_HISTORY level, DEC-019, normalization-v0.5 — the first non-WGI level signal), Sprint 5.11 (credit-to-GDP gap evidence audit, DEC-020 — family reclassified to ONE_SIDED_VULNERABILITY, no score), and Sprint 5.12 (credit-gap ONE_SIDED_VULNERABILITY level, DEC-021 — the owner-approved curve implemented, normalization-v0.6, the second non-WGI level signal), and Sprint 5.13 (confidence layering + WGI uncertainty audit, DEC-022 — architecture/methodology only: no code change, no version bump, confidence stays None, nothing ingested; the indicator_diagnostics storage + ingestion is specified for Sprint 5.14 pending owner acceptance) complete 2026-09-09. Next: owner chooses Sprint 5.14.

## Next milestone

Sprint 5.14 — owner to choose. Primary candidate (the DEC-022 recommendation): WGI uncertainty storage + ingestion — (1) Alembic migration for `indicator_diagnostics` (country, base canonical indicator, diagnostic kind ci_lower_bound/ci_upper_bound/source_count, provider series code, period, value, retrieved-at, vintage; NO SourceSeries rows); (2) a WB fetch path for `GOV_WGI_{RL,CC,PV}.SC_LB/.SC_UB/.SR` persisting ONLY into the diagnostics table (never persist_observations), idempotent + revision-aware, recorded as IngestionRun; (3) a derived-layer lookup helper — diagnostics at the ALIGNED source period (same country/dimension/period, latest vintage; missing → None); (4) nothing else — no confidence formula, no NormalizedSignal change, no model-version bump, no public API. Other candidates: (a) Gini calibration-universe decision (global empirical history vs tracked_8; survey-base comparability); (b) productivity expanded calibration universe (OECD-wide distribution decision — tracked_8 min-max explicitly rejected); (c) data-side work: education Option C (DEC-007), IMF WEO general-government debt (DEC-008), WID wealth shares (lifts the Wealth-gap PARTIAL ceiling). Deliberately NOT queued: DSR momentum (registry 4q/8q windows unapproved), credit-gap momentum (registry 4q/8q windows unapproved; needs a per-indicator sign convention), non-WGI relative scores (cross-country raw-DSR ranking prohibited per BIS caution; credit-gap relative stays CONTEXTUAL_DEFERRED).

## Last successful test

`pytest` in apps/api — 318 passed, 0 failed (2026-09-09, Sprint 5.13: NO production code changed — the suite re-verified the unchanged Sprint 5.12 baseline: WGI level/momentum/relative, DSR level, credit-gap level all unchanged; confidence remains None on every signal; no force scores; no raw mutations; all offline). Sprint 5.13 live verification read-only (nothing written, no DB access): a temporary WB API v2 probe of the WGI uncertainty series — metadata for GOV_WGI_{RL,CC,PV}.SC_LB/.SC_UB/.SE/.SR confirmed as source id 3 (Worldwide Governance Indicators) with CI bounds "for the governance score" and SE "of the governance estimate"; data fetch for all tracked_8, 1996–2025: 208 obs per series (8 × 26), one-to-one year alignment with the score series PERFECT for every country/dimension, LB ≤ score ≤ UB for all 624 points (bounds on the same 0–100 score scale, symmetric, UB clamped at 100), SE on the estimate scale (~0.15–0.25) with width ≈ 56.5 × SE (not 65.8), SR integer counts 4–16; sample 2024 CHE RL: score 87.32 / LB 82.04 / UB 92.60 / width 10.56 / SE 0.1871 / SR 10. The temporary probe script was deleted after the run. No frontend files touched in 5.13 → `npm run build` not re-run (last SUCCESS 2026-09-08, M5.4).